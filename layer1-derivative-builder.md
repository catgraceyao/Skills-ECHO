# Derivative Builder 详细设计文档

> **产品名称**: Derivative Builder (衍生作品构建器)  
> **所属体系**: ECHO 原生分布式价值网络 - 第一层"创作者工具矩阵"  
> **设计版本**: v1.0  
> **设计日期**: 2026-04-19  
> **状态**: 详细设计阶段

---

## 目录

1. [产品定位与核心概念](#1-产品定位与核心概念)
2. [功能模块详细设计](#2-功能模块详细设计)
3. [核心交互流程](#3-核心交互流程)
4. [数据流架构](#4-数据流架构)
5. [可视化编排设计](#5-可视化编排设计)
6. [收益清算机制](#6-收益清算机制)
7. [技术架构与实现](#7-技术架构与实现)
8. [功能清单与工作估算](#8-功能清单与工作估算)

---

## 1. 产品定位与核心概念

### 1.1 设计理念

**Derivative Builder** 是 ECHO 协议创作者工具矩阵中的**第四大核心工具**，它实现了"可组合的创作"这一核心范式转变。在传统的创作模式中，创作者需要从零开始构建作品；而在 ECHO 生态中，创作者可以像搭积木一样组合现有的 Skill、资产和模板，快速构建新的衍生作品。

**核心洞察**: 创作的很大一部分价值来自于对现有元素的重新组合、转换和再创造，而非纯粹的原始创造。Derivative Builder 将这一过程工程化、可视化、自动化。

### 1.2 目标用户画像

| 用户类型 | 特征描述 | 核心需求 | 使用场景 |
|---------|---------|---------|---------|
| **混搭创作者 (Mashup Creator)** | 擅长将不同来源的内容重新组合创作 | 低门槛组合工具、清晰的权利边界、自动收益分配 | 制作音乐混音、视频混剪、AI 艺术融合 |
| **AI 应用开发者** | 构建基于 AI 的自动化工作流 | 标准化 Skill 接口、链式调用、API 集成 | 构建自动化营销内容生成器、智能客服系统 |
| **策展人 (Curator)** | 筛选、组织并包装现有内容 | 预览能力、批量管理、授权状态可视化 | 制作精选集、主题 playlist、资源包 |
| **技能组合者** | 专注将多个小工具组合成大工具 | 可视化编排、参数映射、调试工具 | 构建复杂的自动化工作流、数据处理管道 |
| **商业内容生产者** | 需要快速生产大量衍生内容 | 模板复用、批量处理、成本可控 | 电商商品图批量生成、营销素材快速迭代 |

### 1.3 "可组合创作"的范式转变

```
传统创作流程                    ECHO 可组合创作流程
────────────────────────────────────────────────────────────────────────
从零开始创作     ──────▶       从资产库发现 + 从市场导入
     │                              │
     ▼                              ▼
工具链集成      ──────▶       可视化拖拽编排
(手动配置)                    (自动连接与映射)
     │                              │
     ▼                              ▼
导出成品       ──────▶       实时预览 + 动态调整
     │                              │
     ▼                              ▼
手动处理版权    ──────▶       自动收益分配预览
(复杂且易错)                   (透明且自动化)
     │                              │
     ▼                              ▼
单一作品价值    ──────▶       网络效应价值
(线性增长)                    (指数级增长)
```

**范式转变的关键维度**:

1. **创作起点**: 从"白纸"到"积木" —— 不再从零开始，而是站在巨人肩膀上
2. **创作工具**: 从"单一工具"到"工具组合" —— Skill 成为可组合的原子单元
3. **价值分配**: 从"事后追溯"到"事前约定" —— 每一笔交易自动清算上游
4. **作品形态**: 从"静态结果"到"动态配方" —— 衍生作品本身是可执行的 Blueprint

### 1.4 Skill 作为基本组合单元

**Skill 在 ECHO 协议中的定义**:

```typescript
interface Skill {
  // 身份标识
  id: string;                    // ECHO 资产唯一标识
  version: string;               // 语义化版本
  namespace: string;             // 命名空间 (如 "echo.audio", "user.custom")
  
  // 元数据
  metadata: {
    name: string;
    description: string;
    author: string;              // 创作者 DID
    createdAt: number;
    updatedAt: number;
    tags: string[];
    category: SkillCategory;
    icon?: string;
    preview?: string;
  };
  
  // 接口定义 (详见第4章)
  interface: SkillInterface;
  
  // 执行配置
  execution: {
    runtime: 'sandbox' | 'remote' | 'hybrid';
    resources: ResourceRequirements;
    timeout: number;
    retryPolicy: RetryPolicy;
  };
  
  // 权利配置
  rights: RightsConfiguration;   // ECHO 四维权利模型
}
```

**Skill 作为组合单元的特性**:

| 特性 | 说明 | 组合价值 |
|-----|------|---------|
| **原子性** | 每个 Skill 是功能的最小可执行单元 | 可以精确控制组合粒度 |
| **封装性** | 内部实现对外部完全隐藏 | 降低组合复杂度，保护知识产权 |
| **可复用性** | 一次开发，多次组合使用 | 最大化创作者收益 |
| **可验证性** | 输入输出类型严格定义 | 在编排阶段即可发现类型错误 |
| **可追溯性** | 每个 Skill 都有完整的 ECHO 资产身份 | 权利清算自动追踪 |

### 1.5 核心概念定义

| 概念 | 定义 | 类比 |
|-----|------|-----|
| **Blueprint** | 描述衍生作品如何构建的可执行配方，包含 Skill 组合图、参数配置、权利声明 | 建筑蓝图 / 食谱 |
| **Asset Node** | 代表一个具体的 ECHO 资产（音频、图像、视频等），是 Skill 的输入源 | 食材 |
| **Skill Node** | 代表一个可执行的功能单元，接收输入并产生输出 | 厨具 / 烹饪步骤 |
| **Output Node** | 代表衍生作品的最终输出点，定义导出格式和质量 | 成品盘 |
| **Connection** | 连接两个节点的数据流，表示数据从源到目标的传递 | 管道 |
| **Derivative** | 通过 Blueprint 执行后产生的实际作品 | 成品菜肴 |
| **Upstream** | 某个衍生作品直接或间接依赖的所有原始资产和 Skill | 上游供应商 |

---

## 2. 功能模块详细设计

### 2.1 模块总览

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        Derivative Builder 架构                              │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │
│  │  资产发现    │  │ 可视化编排   │  │  预览渲染    │  │  收益清算    │     │
│  │  & 导入      │  │   引擎       │  │   引擎       │  │   引擎       │     │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘     │
│         │                 │                 │                 │             │
│         ▼                 ▼                 ▼                 ▼             │
│  ┌──────────────────────────────────────────────────────────────────┐     │
│  │                         核心数据层                                │     │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐            │     │
│  │  │ 资产图谱 │ │ 编排状态 │ │ 执行缓存 │ │ 权利配置 │            │     │
│  │  │ 管理器   │ │  管理器  │ │  管理器  │ │  管理器  │            │     │
│  │  └──────────┘ └──────────┘ └──────────┘ └──────────┘            │     │
│  └──────────────────────────────────────────────────────────────────┘     │
│         │                 │                 │                 │             │
│         ▼                 ▼                 ▼                 ▼             │
│  ┌──────────────────────────────────────────────────────────────────┐     │
│  │                         ECHO 协议层                               │     │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐            │     │
│  │  │ ECHO 资产 │ │ 权利验证 │ │ 收益分配 │ │ 链上确权 │            │     │
│  │  │  查询    │ │   API    │ │  合约    │ │   API    │            │     │
│  │  └──────────┘ └──────────┘ └──────────┘ └──────────┘            │     │
│  └──────────────────────────────────────────────────────────────────┘     │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 2.2 资产发现与导入模块

#### 2.2.1 功能描述

资产发现模块是创作者进入 Derivative Builder 的起点，它连接 ECHO 协议的全局资产图谱，提供强大的搜索、筛选和预览能力。

#### 2.2.2 核心功能

**1. 多维度资产搜索**

```typescript
interface AssetSearchOptions {
  // 基础搜索
  query: string;                    // 关键词搜索
  types: AssetType[];              // 资产类型过滤 (audio, image, video, skill, template)
  
  // 创作者维度
  authors: string[];               // 指定创作者
  verifiedOnly: boolean;           // 仅显示认证创作者
  following: boolean;              // 仅显示关注创作者的资产
  
  // 权利维度
  derivativeAllowed: boolean;      // 允许衍生创作
  commercialUse: boolean;          // 允许商业使用
  maxRoyaltyRate: number;          // 最大可接受版税率
  
  // 质量维度
  minQuality: number;              // 最低质量评分 (0-100)
  minUsage: number;                // 最低使用次数
  sortBy: 'relevance' | 'popular' | 'newest' | 'royalty_asc';
  
  // 时间维度
  createdAfter: number;
  createdBefore: number;
}
```

**2. 智能推荐引擎**

- **协同过滤**: 基于相似创作者的资产使用历史推荐
- **内容相似**: 基于视觉/音频特征向量推荐相似资产
- **趋势发现**: 推荐当前热度上升的资产
- **组合建议**: 基于已有选择推荐兼容资产

**3. 实时预览与授权检查**

```
┌─────────────────────────────────────────────────────────┐
│                    资产卡片预览                          │
├─────────────────────────────────────────────────────────┤
│ ┌─────────────┐  ┌─────────────────────────────────┐   │
│ │             │  │ 资产名称: Lo-fi Hip Hop Beat    │   │
│ │   [预览     │  │ 创作者: @jazzmaster ■ 已认证    │   │
│ │    播放器]  │  │ 类型: Audio / 采样库            │   │
│ │             │  │                                 │   │
│ └─────────────┘  │ ┌─────────────────────────────┐ │   │
│                  │ │ 权利配置                     │ │   │
│                  │ │ • 允许衍生: ✓               │ │   │
│                  │ │ • 商业使用: ✓ (需 8% 版税)  │ │   │
│                  │ │ • 归属要求: 必须署名        │ │   │
│                  │ │ • 修改范围: 允许重度修改    │ │   │
│                  │ └─────────────────────────────┘ │   │
│                  │                                 │   │
│                  │ 热度: ████████░░ 80 | 使用 1.2k │   │
│                  │                                 │   │
│                  │ [查看详情] [测试运行] [导入]     │   │
└─────────────────────────────────────────────────────────┘
```

**4. 批量导入与智能分类**

- 支持一次选择多个资产自动导入
- 根据资产类型自动分类到对应面板
- 检测重复导入并提示
- 自动下载缩略图和元数据到本地缓存

#### 2.2.3 技术实现

| 组件 | 技术选型 | 说明 |
|-----|---------|------|
| 搜索索引 | Elasticsearch + 向量数据库 | 支持全文搜索 + 语义搜索 |
| 预览引擎 | Web Audio API / WebGL / HLS | 根据资产类型选择渲染方式 |
| 缓存策略 | IndexedDB + LRU | 本地缓存预览数据和元数据 |
| 授权验证 | ECHO Rights API | 实时查询资产的授权状态 |

### 2.3 可视化编排模块

#### 2.3.1 功能描述

可视化编排器是 Derivative Builder 的核心界面，采用节点编辑器范式，让创作者通过拖拽和连线的方式构建复杂的 Skill 组合逻辑。

#### 2.3.2 界面布局

```
┌─────────────────────────────────────────────────────────────────────────────┐
│ [菜单] [文件▼] [编辑▼] [视图▼] [运行▼] [ECHO▼]         [登录/用户头像]     │
├───────────────────────┬─────────────────────────────────────────────────────┤
│                       │                                                     │
│  ┌─────────────────┐  │  ┌─────────────────────────────────────────────┐   │
│  │   资产面板       │  │  │                                             │   │
│  │                  │  │  │           主画布 (无限画布)                  │   │
│  │  📁 项目资产     │  │  │                                             │   │
│  │  ├─ 🎵 音频      │  │  │    ┌────────┐         ┌────────┐          │   │
│  │  ├─ 🖼️ 图像      │  │  │    │ Asset  │────▶───│ Skill  │────┐     │   │
│  │  ├─ 🎬 视频      │  │  │    │ Node A │         │ Node 1 │    │     │   │
│  │  ├─ 🔧 Skills   │  │  │    └────────┘         └────────┘    │     │   │
│  │  └─ 📋 模板      │  │  │                                       │     │   │
│  │                  │  │  │    ┌────────┐         ┌────────┐     ▼     │   │
│  │  🔍 搜索资产...  │  │  │    │ Asset  │────▶───│ Skill  │──▶┌────┐  │   │
│  │                  │  │  │    │ Node B │         │ Node 2 │   │    │  │   │
│  │  ───────────────  │  │  │    └────────┘         └────────┘   │Out │  │   │
│  │                  │  │  │                                     │put │  │   │
│  │  ⭐ 推荐资产     │  │  │                                     │Node│  │   │
│  │  ├─ Skill X      │  │  │                                     └────┘  │   │
│  │  ├─ Asset Y      │  │  │                                             │   │
│  │  └─ Template Z   │  │  └─────────────────────────────────────────────┘   │
│  │                  │  │                                                     │
│  └─────────────────┘  │  ┌─────────────────────────────────────────────┐   │
│                       │  │  底部面板 (属性/日志/预览/权利)               │   │
│                       │  │  [属性 ▼] [日志] [预览] [权利树] [性能]        │   │
│                       │  │                                             │   │
│                       │  │  选中节点: Skill Node 1                     │   │
│                       │  │  ┌─────────────────────────────────────┐    │   │
│                       │  │  │ 参数配置:                             │    │   │
│                       │  │  │ • 强度: [────●────] 65%              │    │   │
│                       │  │  │ • 模式: [下拉: 标准 ▼]               │    │   │
│                       │  │  │ • 保留元数据: [☑]                    │    │   │
│                       │  │  └─────────────────────────────────────┘    │   │
│                       │  └─────────────────────────────────────────────┘   │
└───────────────────────┴─────────────────────────────────────────────────────┘
```

#### 2.3.3 节点类型详解

**资产节点 (Asset Node)**

```typescript
interface AssetNode {
  type: 'asset';
  id: string;
  assetId: string;           // 引用的 ECHO 资产 ID
  
  // 显示配置
  display: {
    position: { x: number; y: number };
    size: { width: number; height: number };
    color: string;           // 根据资产类型自动分配
    collapsed: boolean;      // 是否折叠显示
  };
  
  // 输出端口 (资产本身作为输出)
  output: Port;
  
  // 片段选择 (用于只使用资产的一部分)
  segment?: {
    start: number;
    end: number;
  };
}
```

**Skill 节点 (Skill Node)**

```typescript
interface SkillNode {
  type: 'skill';
  id: string;
  skillId: string;           // 引用的 Skill ID
  
  // 显示配置
  display: {
    position: { x: number; y: number };
    expanded: boolean;       // 是否展开显示内部
  };
  
  // 端口 (动态根据 Skill 接口定义生成)
  inputs: Port[];
  outputs: Port[];
  configPorts: ConfigPort[]; // 配置参数端口
  
  // 参数值
  parameters: Record<string, any>;
  
  // 执行状态
  execution: {
    state: 'idle' | 'running' | 'success' | 'error';
    progress: number;
    lastResult?: ExecutionResult;
    errorMessage?: string;
  };
}
```

**输出节点 (Output Node)**

```typescript
interface OutputNode {
  type: 'output';
  id: string;
  
  // 显示配置
  display: {
    position: { x: number; y: number };
  };
  
  // 输入端口
  inputs: Port[];
  
  // 导出配置
  export: {
    format: string;
    quality: 'draft' | 'standard' | 'high' | 'master';
    metadata: boolean;       // 是否包含 ECHO 元数据
  };
}
```

#### 2.3.4 交互设计

| 操作 | 交互方式 | 反馈 |
|-----|---------|------|
| 添加节点 | 从侧边栏拖拽 / 双击空白处 | 节点出现在画布中央，自动选中 |
| 创建连线 | 从输出端口拖拽到输入端口 | 实时预览连线，类型匹配时高亮 |
| 删除连线 | 选中连线按 Delete / 右键删除 | 连线消失，下游节点状态更新 |
| 调整参数 | 属性面板滑动条 / 节点内嵌控件 | 实时预览更新（如果支持） |
| 运行节点 | 右键菜单 / 工具栏按钮 | 节点边框闪烁，进度条显示 |
| 分组 | 选中多个节点右键"分组" | 创建组节点，可折叠 |
| 撤销重做 | Ctrl+Z / Ctrl+Y | 状态回退/前进 |

### 2.4 Skill 市场集成模块

#### 2.4.1 功能描述

Skill 市场集成让创作者可以直接在 Derivative Builder 中浏览、测试和使用 ECHO Skill 市场中的所有 Skill，无需离开创作环境。

#### 2.4.2 功能特性

**1. 内嵌市场浏览器**

```
┌─────────────────────────────────────────────────────────────┐
│  🔍 搜索 Skills...    [分类 ▼] [排序 ▼] [筛选 ▼]    [刷新]  │
├─────────────────────────────────────────────────────────────┤
│ ┌────────────┐ ┌────────────┐ ┌────────────┐               │
│ │ 🔧         │ │ 🎨         │ │ 📝         │               │
│ │            │ │            │ │            │               │
│ │ Audio      │ │ Image      │ │ Text       │               │
│ │ Enhancer   │ │ Generator  │ │ Analyzer   │               │
│ │            │ │            │ │            │               │
│ │ ⭐ 4.8     │ │ ⭐ 4.9     │ │ ⭐ 4.6     │               │
│ │ 💰 免费    │ │ 💰 2%/次   │ │ 💰 1%/次   │               │
│ │            │ │            │ │            │               │
│ │ [预览]     │ │ [预览]     │ │ [预览]     │               │
│ │ [添加到画布]│ │ [添加到画布]│ │ [添加到画布]│               │
│ └────────────┘ └────────────┘ └────────────┘               │
└─────────────────────────────────────────────────────────────┘
```

**2. 一键导入与测试**

- 点击"添加到画布"自动创建 Skill 节点
- 首次使用 Skill 时弹出参数说明和示例
- 内置测试模式：用示例数据运行 Skill 查看效果
- 支持"试用后购买"模式（有限制次数）

**3. 版本管理与更新**

- 自动检测已使用 Skill 的新版本
- 提供一键更新或保持旧版本选项
- 版本差异对比显示
- 更新前自动备份当前 Blueprint

### 2.5 预览与渲染模块

#### 2.5.1 功能描述

预览渲染模块提供实时或近实时的效果预览，让创作者在发布前就能看到最终作品的效果。

#### 2.5.2 渲染模式

| 模式 | 描述 | 适用场景 | 性能开销 |
|-----|------|---------|---------|
| **草稿预览** | 低质量快速预览 | 编排过程中的频繁检查 | 低 |
| **标准预览** | 正常质量预览 | 阶段性检查 | 中 |
| **高质量预览** | 接近最终质量 | 发布前最终确认 | 高 |
| **完整渲染** | 最终质量输出 | 生成发布资产 | 最高 |

#### 2.5.3 实时预览机制

```
┌─────────────────────────────────────────────────────────────────┐
│                     实时预览流水线                               │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  参数变更 ──▶ 依赖分析 ──▶ 智能缓存 ──▶ 增量渲染 ──▶ 预览更新    │
│                │            │          │                       │
│                ▼            ▼          ▼                       │
│           识别受影响     检查哪些     只重新计算                 │
│           的节点         节点可复用   变化的节点                 │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

**关键优化策略**:

1. **依赖图缓存**: 构建完整的节点依赖图，参数变更时只标记受影响的节点
2. **中间结果缓存**: 缓存每个节点的输出，上游未变更时直接复用
3. **懒加载**: 视口外的节点不渲染预览
4. **Web Worker**: 渲染计算在 Worker 中进行，不阻塞 UI
5. **渐进式渲染**: 先显示低质量预览，再逐步提升质量

### 2.6 收益分配预览模块

#### 2.6.1 功能描述

收益分配预览是 ECHO 协议的核心创新之一，它让创作者在发布前就能清楚地看到每一笔销售收入将如何分配，确保完全的透明度。

#### 2.6.2 分配树可视化

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        收益分配预览                                          │
│                   假设售价: $100.00                                          │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│                              ┌─────────────┐                                │
│                              │  最终收益    │                                │
│                              │   $64.50    │                                │
│                              │   (64.5%)   │                                │
│                              └──────┬──────┘                                │
│                                     │                                       │
│                    ┌────────────────┼────────────────┐                      │
│                    │                │                │                      │
│                    ▼                ▼                ▼                      │
│              ┌──────────┐    ┌──────────┐    ┌──────────┐                  │
│              │ 上游资产A │    │ 上游资产B │    │ 上游Skill│                  │
│              │  $15.00  │    │  $8.00   │    │  $12.50  │                  │
│              │  (15%)   │    │  (8%)    │    │ (12.5%)  │                  │
│              └────┬─────┘    └────┬─────┘    └──────────┘                  │
│                   │               │                                         │
│           ┌───────┘               └───────┐                                │
│           ▼                               ▼                                │
│     ┌──────────┐                    ┌──────────┐                           │
│     │ 资产A的上 │                    │ 资产B的上 │                           │
│     │ 游创作者 │                    │ 游创作者 │                           │
│     │  $12.00  │                    │  $6.00   │                           │
│     └──────────┘                    └──────────┘                           │
│                                                                             │
│  平台服务费: $10.00 (10%)  │  智能合约费: $0.50 (0.5%)                     │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

#### 2.6.3 交互功能

- **价格模拟**: 调整假设售价，实时更新所有分配金额
- **敏感性分析**: 查看不同价格点下的收益变化
- **对比模式**: 对比不同组合方案的收益差异
- **导出报告**: 生成 PDF/CSV 格式的收益分配报告

### 2.7 发布与确权模块

#### 2.7.1 功能描述

发布模块负责将 Blueprint 和渲染结果上传到 ECHO 网络，完成新资产的确权和注册。

#### 2.7.2 发布流程

```
┌─────────┐   ┌─────────┐   ┌─────────┐   ┌─────────┐   ┌─────────┐
│  预检查  │──▶│ 元数据  │──▶│ 权利配置 │──▶│ 链上提交 │──▶│ 完成确认 │
└─────────┘   └─────────┘   └─────────┘   └─────────┘   └─────────┘
     │            │            │            │            │
     ▼            ▼            ▼            ▼            ▼
• 引用完整性  • 标题描述   • 四维权利    • 签名提交   • 资产 ID
• 授权验证    • 标签分类   • 收益分配    • 支付 Gas   • 交易哈希
• 必填字段    • 预览图     • 许可条款    • 等待确认   • 分享链接
```

#### 2.7.3 发布选项

| 选项 | 说明 | 默认 |
|-----|------|-----|
| **可见性** | Public / Unlisted / Private | Public |
| **衍生许可** | 是否允许他人基于此创作 | 允许 |
| **商业使用** | 是否允许商业用途 | 允许 |
| **版税比例** | 二次销售分成比例 | 5% |
| **初始定价** | 首次销售价格 | 用户设定 |
| **复制保护** | 是否启用防复制措施 | 启用 |

---

## 3. 核心交互流程

### 3.1 主流程状态图

```
                              ┌─────────────┐
                              │   开始      │
                              └──────┬──────┘
                                     │
                                     ▼
                    ┌────────────────────────────────┐
                    │         创建新项目              │
                    │  ┌──────────┐  ┌──────────┐   │
                    │  │ 空白项目 │  │ 从模板   │   │
                    │  └──────────┘  └──────────┘   │
                    └────────────────┬───────────────┘
                                     │
                                     ▼
                    ┌────────────────────────────────┐
                    │        资产发现与导入           │
                    │  • 搜索 ECHO 资产              │
                    │  • 验证授权状态                │
                    │  • 添加到画布                  │
                    └────────────────┬───────────────┘
                                     │
                                     ▼
                    ┌────────────────────────────────┐
                    │      可视化编排与配置           │
                    │  • 添加 Skill 节点             │
                    │  • 连接数据流                  │
                    │  • 配置参数                    │
                    └────────────────┬───────────────┘
                                     │
                                     ▼
                    ┌────────────────────────────────┐
                    │        预览与调试               │
                    │  • 实时预览效果                │
                    │  • 调整优化                    │
                    │  • 测试完整流程                │
                    └────────────────┬───────────────┘
                                     │
                                     ▼
                    ┌────────────────────────────────┐
                    │      收益分配确认               │
                    │  • 查看上游资产                │
                    │  • 确认分润比例                │
                    │  • 模拟不同价格                │
                    └────────────────┬───────────────┘
                                     │
                                     ▼
                    ┌────────────────────────────────┘
                    │         发布确权
                    │  • 填写元数据
                    │  • 配置权利
                    │  • 链上提交
                    └────────────────┬───────────────┘
                                     │
                                     ▼
                              ┌─────────────┐
                              │   完成      │
                              │ 获得资产 ID  │
                              └─────────────┘
```

### 3.2 详细交互流程

#### 3.2.1 创建新项目

**流程步骤**:

1. **入口选择**
   - 点击"新建项目"按钮
   - 弹出选择对话框: 空白项目 / 从模板创建 / 导入现有

2. **空白项目**
   - 打开空画布
   - 显示引导提示: "从左侧资产面板拖拽开始"
   - 自动生成项目 ID 和时间戳

3. **从模板创建**
   - 显示模板库 (个人模板 / 社区模板 / 官方模板)
   - 分类: 音频混音模板、图像处理链、视频工作流、AI 工作流
   - 选择后克隆模板，打开可编辑副本

4. **导入现有**
   - 支持导入: ECHO Blueprint 文件 (.echobp)
   - 支持导入: 兼容格式 (ComfyUI JSON, n8n 工作流等)
   - 自动映射兼容节点，不兼容节点显示警告

#### 3.2.2 搜索并导入资产

**流程步骤**:

1. **打开资产发现**
   - 点击侧边栏"发现资产"标签
   - 或按快捷键 `Ctrl/Cmd + F`

2. **输入搜索条件**
   - 关键词搜索 (实时建议)
   - 筛选面板: 类型、权利、创作者、时间
   - 智能标签: 热门、趋势、兼容、推荐

3. **预览资产**
   - 悬停显示预览卡片
   - 点击展开详细预览 (可播放/查看)
   - 显示权利配置和使用统计

4. **授权检查**
   - 系统自动检查当前用户是否有权使用
   - 无权使用: 显示购买/授权按钮
   - 有权使用: 显示"导入"按钮

5. **导入到画布**
   - 点击导入后，资产节点出现在画布中央
   - 自动选择并显示属性面板
   - 资产添加到项目资产列表

#### 3.2.3 配置 Skill 链

**流程步骤**:

1. **添加 Skill 节点**
   - 从侧边栏拖拽 Skill 到画布
   - 或右键空白处"添加节点"搜索添加

2. **连接数据流**
   - 从输出端口拖拽到输入端口
   - 系统自动验证类型兼容性
   - 不兼容时显示红色警告和原因

3. **配置参数**
   - 选中 Skill 节点
   - 在属性面板调整参数
   - 或使用节点内嵌快速控件
   - 复杂参数可展开子面板

4. **配置映射**
   - 支持上游输出值作为参数输入
   - 拖拽端口到参数字段建立映射
   - 支持简单的表达式转换

5. **测试运行**
   - 右键节点"测试此节点"
   - 系统运行上游依赖并显示结果
   - 错误时显示详细日志

#### 3.2.4 预览组合效果

**流程步骤**:

1. **触发预览**
   - 点击工具栏"预览"按钮
   - 或按快捷键 `Space`

2. **执行渲染**
   - 系统分析依赖图
   - 检查缓存命中
   - 执行必要的节点

3. **显示结果**
   - 输出节点处显示预览
   - 底部面板切换到预览标签
   - 显示渲染进度和预估时间

4. **迭代调整**
   - 调整参数后自动触发重新预览
   - 使用滑块进行 A/B 对比
   - 保存多个版本快照

#### 3.2.5 设置衍生权利

**流程步骤**:

1. **打开权利配置**
   - 点击工具栏"权利"按钮
   - 或按 `Ctrl/Cmd + R`

2. **配置四维权利**
   - 复制权: 是否允许复制
   - 商业使用权: 是否允许商业用途
   - 修改权: 允许修改的范围
   - 再创作权: 是否允许作为新作品的基础

3. **设置版税比例**
   - 调整二次销售分成比例
   - 设置不同使用场景的费率
   - 预览不同价格下的分配

4. **冲突检测**
   - 系统自动检测上游权利冲突
   - 显示冲突列表和建议解决方案
   - 必须解决所有冲突才能发布

#### 3.2.6 收益分配确认

**流程步骤**:

1. **生成分配树**
   - 系统自动扫描所有上游引用
   - 递归获取上游资产的权利配置
   - 构建完整的分配树

2. **可视化展示**
   - 以树状图显示分配关系
   - 每个节点显示名称和分配比例
   - 悬停显示详细信息

3. **价格模拟**
   - 调整售价滑块
   - 实时更新所有金额
   - 显示创作者最终收益

4. **确认与同意**
   - 显示分配摘要
   - 创作者确认接受
   - 记录同意时间戳

#### 3.2.7 发布上链

**流程步骤**:

1. **预检查**
   - 验证所有引用资产的有效性
   - 检查权利配置完整性
   - 验证必填字段

2. **元数据填写**
   - 标题、描述、标签
   - 分类选择
   - 预览图上传/生成

3. **最终渲染**
   - 执行完整渲染流程
   - 生成所有输出文件
   - 上传至分布式存储

4. **链上提交**
   - 生成资产 DID
   - 提交权利配置
   - 支付 Gas 费用
   - 等待确认

5. **完成**
   - 显示资产 ID 和交易哈希
   - 提供分享链接
   - 询问是否查看资产页面

---

## 4. 数据流架构

### 4.1 Skill 接口标准

#### 4.1.1 接口定义

```typescript
interface SkillInterface {
  // 接口版本
  version: '1.0.0';
  
  // 输入定义
  inputs: InputPort[];
  
  // 输出定义
  outputs: OutputPort[];
  
  // 配置参数定义
  parameters: ParameterDef[];
  
  // 执行元数据
  execution: ExecutionMetadata;
}

interface InputPort {
  id: string;                    // 端口唯一标识
  name: string;                  // 显示名称
  type: DataType;               // 数据类型
  required: boolean;            // 是否必填
  default?: DataValue;          // 默认值
  description: string;          // 描述
  constraints?: TypeConstraints; // 类型约束
}

interface OutputPort {
  id: string;
  name: string;
  type: DataType;
  description: string;
  // 输出不需要 required，Skill 始终产生输出
}

interface ParameterDef {
  id: string;
  name: string;
  type: ParameterType;
  default: any;
  description: string;
  constraints?: {
    min?: number;
    max?: number;
    step?: number;
    options?: string[];
    regex?: string;
  };
  ui?: {
    widget: 'slider' | 'dropdown' | 'text' | 'number' | 'checkbox' | 'color' | 'file';
    group?: string;
    order?: number;
  };
}
```

#### 4.1.2 数据类型系统

```typescript
// 基础类型
type PrimitiveType = 
  | 'string'      // 文本
  | 'number'      // 数值
  | 'boolean'     // 布尔
  | 'color'       // 颜色 (hex/rgba)
  | 'datetime';   // 日期时间

// 媒体类型
type MediaType =
  | 'audio'       // 音频 (包含格式信息)
  | 'image'       // 图像 (包含尺寸/格式)
  | 'video'       // 视频 (包含时长/分辨率)
  | '3d'          // 3D 模型
  | 'document';   // 文档

// 结构化类型
type StructuredType =
  | 'array<T>'    // 数组
  | 'map<K,V>'    // 键值映射
  | 'json'        // 任意 JSON
  | 'binary';     // 二进制数据

// ECHO 特定类型
type EchoType =
  | 'echo.asset'  // ECHO 资产引用
  | 'echo.skill'  // Skill 引用
  | 'echo.rights'; // 权利配置

// 完整类型定义
type DataType = PrimitiveType | MediaType | StructuredType | EchoType;
```

#### 4.1.3 类型转换规则

```
自动转换 (隐式)
─────────────────────────────────────────────────
string ──▶ number     (解析数字)
number ──▶ string     (数字转字符串)
image  ──▶ video      (单帧视频)
audio  ──▶ video      (添加占位画面)
color  ──▶ string     (hex 表示)

需要显式转换
─────────────────────────────────────────────────
video  ──▶ image      (需要指定帧)
audio  ──▶ string     (需要指定转录)
binary ──▶ any        (需要指定解析方式)
```

### 4.2 执行引擎架构

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         执行引擎架构                                     │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │                        调度器 (Scheduler)                        │   │
│  │  • 分析依赖图，确定执行顺序                                       │   │
│  │  • 管理并发执行，控制资源使用                                     │   │
│  │  • 处理重试和错误恢复                                             │   │
│  └────────────────────────────┬────────────────────────────────────┘   │
│                               │                                         │
│           ┌───────────────────┼───────────────────┐                    │
│           ▼                   ▼                   ▼                    │
│  ┌─────────────────┐ ┌─────────────────┐ ┌─────────────────┐          │
│  │   沙箱执行器     │ │   远程执行器     │ │   混合执行器     │          │
│  │                 │ │                 │ │                 │          │
│  │ • WASM 运行时   │ │ • API 调用      │ │ • 本地+远程混合 │          │
│  │ • JS 沙箱       │ │ • WebSocket     │ │ • 智能路由      │          │
│  │ • 资源隔离      │ │ • 队列管理      │ │ • 故障转移      │          │
│  └─────────────────┘ └─────────────────┘ └─────────────────┘          │
│                                                                         │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │                        缓存层                                    │   │
│  │  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐               │   │
│  │  │ 输入缓存    │ │ 输出缓存    │ │ 中间结果    │               │   │
│  │  │ (输入哈希)  │ │ (输出签名)  │ │ (节点输出)  │               │   │
│  │  └─────────────┘ └─────────────┘ └─────────────┘               │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

#### 4.2.1 执行模式详解

**沙箱模式 (Sandbox)**

```typescript
interface SandboxConfig {
  runtime: 'wasm' | 'javascript' | 'python';
  
  // 资源限制
  limits: {
    memory: number;        // 最大内存 (MB)
    cpu: number;          // CPU 时间限制 (ms)
    storage: number;      // 临时存储 (MB)
    network: boolean;     // 是否允许网络
  };
  
  // 安全策略
  security: {
    filesystem: 'none' | 'tmp' | 'isolated';
    network: 'none' | 'echo' | 'restricted' | 'full';
    permissions: string[];
  };
  
  // 超时配置
  timeout: {
    total: number;        // 总超时 (ms)
    startup: number;      // 启动超时 (ms)
    execution: number;    // 执行超时 (ms)
  };
}
```

**远程模式 (Remote)**

```typescript
interface RemoteConfig {
  endpoint: string;       // Skill 服务地址
  protocol: 'http' | 'websocket' | 'grpc';
  
  // 认证
  auth: {
    type: 'none' | 'apikey' | 'oauth' | 'echo-sig';
    credentials: any;
  };
  
  // 重试策略
  retry: {
    maxAttempts: number;
    backoff: 'fixed' | 'linear' | 'exponential';
    initialDelay: number;
  };
  
  // 流传输 (用于大文件)
  streaming: {
    enabled: boolean;
    chunkSize: number;
  };
}
```

### 4.3 缓存策略

#### 4.3.1 缓存层次

```
L1: 内存缓存 (Session)
     └── 当前会话中频繁访问的数据
     └── 失效: 会话结束

L2: 本地缓存 (IndexedDB)
     └── 中间执行结果
     └── 已下载的资产预览
     └── 失效: LRU + 大小限制

L3: 分布式缓存 (ECHO Network)
     └── 热门执行结果
     └── 共享的中间结果
     └── 失效: TTL + 手动清除
```

#### 4.3.2 缓存键生成

```typescript
function generateCacheKey(node: SkillNode, inputs: DataValue[]): string {
  const inputHash = hashInputs(inputs);
  const paramHash = hashParameters(node.parameters);
  const skillVersion = node.skillVersion;
  
  return `skill:${node.skillId}:${skillVersion}:${inputHash}:${paramHash}`;
}

function hashInputs(inputs: DataValue[]): string {
  // 对于小数据直接哈希
  // 对于大文件哈希其内容标识符
  return inputs.map(input => {
    if (isLargeFile(input)) {
      return hash(input.contentId);
    }
    return hash(JSON.stringify(input));
  }).join(':');
}
```

### 4.4 错误处理机制

#### 4.4.1 错误分类

| 错误类型 | 描述 | 处理策略 |
|---------|------|---------|
| **语法错误** | Blueprint 结构错误 | 保存前验证，阻止保存 |
| **类型错误** | 端口类型不匹配 | 连线时实时提示，拒绝连接 |
| **缺失错误** | 必填输入未连接 | 执行前检查，标记缺失 |
| **运行时错误** | Skill 执行失败 | 捕获异常，显示日志 |
| **超时错误** | 执行超过时限 | 中断执行，提示优化 |
| **资源错误** | 内存/存储不足 | 清理缓存，重试或提示 |
| **网络错误** | 远程调用失败 | 自动重试，切换备用 |

#### 4.4.2 错误处理流程

```
执行错误发生
     │
     ▼
┌─────────────────┐
│  错误捕获        │
│  (try-catch)    │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  错误分类        │
│  (ErrorClassifier)│
└────────┬────────┘
         │
    ┌────┴────┐
    ▼         ▼
┌────────┐  ┌────────┐
│可恢复   │  │不可恢复│
└───┬────┘  └───┬────┘
    │           │
    ▼           ▼
┌────────┐  ┌────────┐
│重试策略 │  │记录失败│
│• 立即重试│  │更新状态│
│• 延迟重试│  │显示错误│
│• 降级方案│  │提供帮助│
└────────┘  └────────┘
```

#### 4.4.3 部分成功处理

```typescript
interface ExecutionResult {
  status: 'success' | 'partial' | 'failure';
  
  // 节点级别结果
  nodeResults: {
    [nodeId: string]: {
      status: 'success' | 'skipped' | 'failed';
      output?: DataValue;
      error?: ExecutionError;
      executionTime: number;
    };
  };
  
  // 可用输出 (即使部分失败)
  availableOutputs: {
    [nodeId: string]: DataValue;
  };
  
  // 错误汇总
  errors: ExecutionError[];
  
  // 重试建议
  retrySuggestion?: {
    nodes: string[];
    strategy: string;
  };
}
```

---

## 5. 可视化编排设计

### 5.1 节点系统详细设计

#### 5.1.1 节点状态机

```
                    ┌─────────────┐
         ┌─────────▶│   未初始化   │◀─────────┐
         │          │   (init)    │          │
         │          └──────┬──────┘          │
         │                 │                 │
         │                 ▼                 │
         │          ┌─────────────┐          │
         │    ┌─────│   空闲      │─────┐    │
         │    │     │   (idle)    │     │    │
         │    │     └──────┬──────┘     │    │
         │    │            │            │    │
    配置更新   │       开始执行      检查缓存    │
         │    │            │            │    │
         │    │            ▼            │    │
         │    │     ┌─────────────┐     │    │
         │    └────▶│   执行中    │◀────┘    │
         │          │  (running)  │          │
         │          └──────┬──────┘          │
         │                 │                 │
         │         ┌───────┴───────┐         │
         │         ▼               ▼         │
         │   ┌──────────┐    ┌──────────┐    │
         └──▶│  成功    │    │  失败    │────┘
             │ (success)│    │ (failed) │
             └────┬─────┘    └──────────┘
                  │
                  ▼
             ┌──────────┐
             │ 结果缓存 │
             └──────────┘
```

#### 5.1.2 节点外观规范

```
┌─────────────────────────────────────┐
│ ■ [类型图标] 节点名称            ✕ │  ← 标题栏 (颜色标识类型)
├─────────────────────────────────────┤
│                                     │
│ ● 输入端口1     输出端口1 ●         │  ← 端口行
│   [类型标签]      [类型标签]         │
│                                     │
│ ● 输入端口2     输出端口2 ●         │
│   (可选)          [类型标签]         │
│                                     │
├─────────────────────────────────────┤
│ ⚙️ 配置参数 (可折叠)                │  ← 配置区
│ • 参数1: [────●────]                │
│ • 参数2: [下拉 ▼]                   │
│                                     │
├─────────────────────────────────────┤
│ 📊 执行统计                         │  ← 状态区 (可折叠)
│ 平均耗时: 1.2s | 缓存命中: 80%     │
└─────────────────────────────────────┘
```

**颜色规范**:

| 节点类型 | 主色 | 边框 | 用途 |
|---------|------|------|------|
| 资产节点 | #4A90D9 | #357ABD | 表示输入资产 |
| Skill 节点 | #7B68EE | #6A5ACD | 表示处理逻辑 |
| 输出节点 | #50C878 | #3CB371 | 表示最终输出 |
| 组节点 | #FF6B6B | #EE5A5A | 表示分组 |
| 注释节点 | #FFD93D | #F0C800 | 表示说明文字 |

#### 5.1.3 端口设计

```typescript
interface Port {
  id: string;
  type: DataType;
  
  // 视觉属性
  visual: {
    position: 'left' | 'right' | 'top' | 'bottom';
    color: string;           // 根据类型自动分配
    shape: 'circle' | 'square' | 'triangle';
    size: number;
  };
  
  // 连接状态
  connection: {
    connected: boolean;
    sourceId?: string;       // 连接的源端口
    targetId?: string;       // 连接的目标端口
  };
  
  // 交互
  interaction: {
    allowMultiple: boolean;  // 是否允许多连接
    required: boolean;       // 是否必须连接
    hoverHint: string;       // 悬停提示
  };
}
```

### 5.2 连线系统

#### 5.2.1 连线样式

```typescript
interface ConnectionStyle {
  // 基础样式
  stroke: string;
  strokeWidth: number;
  
  // 动态样式
  animated: boolean;         // 数据流动画
  dashArray?: string;        // 虚线模式
  
  // 状态样式
  states: {
    default: LineStyle;
    hover: LineStyle;
    selected: LineStyle;
    error: LineStyle;       // 类型不匹配时
    active: LineStyle;      // 数据传输时
  };
}
```

#### 5.2.2 类型匹配规则

```
连线验证流程:

拖拽开始 ──▶ 获取源端口类型 ──▶ 遍历所有可连接目标
                              │
                              ▼
                    ┌─────────────────┐
                    │ 目标端口类型匹配? │
                    └────────┬────────┘
                             │
              ┌──────────────┼──────────────┐
              ▼              ▼              ▼
           精确匹配      自动转换        不匹配
              │              │              │
              ▼              ▼              ▼
           绿色高亮     黄色高亮+提示    红色禁用
           允许连接     允许连接        显示原因
```

#### 5.2.3 循环检测

```typescript
function detectCycle(graph: NodeGraph, newConnection: Connection): boolean {
  const visited = new Set<string>();
  const recursionStack = new Set<string>();
  
  function hasCycle(nodeId: string): boolean {
    visited.add(nodeId);
    recursionStack.add(nodeId);
    
    const neighbors = getOutgoingConnections(nodeId);
    for (const neighbor of neighbors) {
      if (!visited.has(neighbor)) {
        if (hasCycle(neighbor)) return true;
      } else if (recursionStack.has(neighbor)) {
        return true;
      }
    }
    
    recursionStack.delete(nodeId);
    return false;
  }
  
  // 添加新连接后检测
  const tempGraph = addConnection(graph, newConnection);
  
  for (const node of tempGraph.nodes) {
    if (!visited.has(node.id)) {
      if (hasCycle(node.id)) return true;
    }
  }
  
  return false;
}
```

### 5.3 分组与层级管理

#### 5.3.1 组节点设计

```typescript
interface GroupNode {
  type: 'group';
  id: string;
  
  // 包含的节点
  children: string[];        // 节点 ID 列表
  
  // 边界
  bounds: {
    x: number;
    y: number;
    width: number;
    height: number;
  };
  
  // 外观
  appearance: {
    title: string;
    color: string;
    collapsed: boolean;
    showPorts: boolean;      // 折叠时是否显示端口
  };
  
  // 端口映射 (外部连接映射到内部节点)
  portMapping: {
    external: Port;
    internal: {
      nodeId: string;
      portId: string;
    };
  }[];
}
```

#### 5.3.2 层级展开/折叠

```
折叠状态:
┌─────────────────────────────┐
│ ▶ 组名称 [3 个节点]     □ │
│ ───────────────────────────  │
│ ● 输入                      │
│                     输出 ●  │
└─────────────────────────────┘

展开状态:
┌─────────────────────────────────────────────┐
│ ▼ 组名称                               □ │
├─────────────────────────────────────────────┤
│  ┌──────────┐      ┌──────────┐            │
│  │ 内部节点1 │─────▶│ 内部节点2 │───┐       │
│  └──────────┘      └──────────┘   │       │
│                                   ▼       │
│                             ┌──────────┐  │
│                             │ 内部节点3 │  │
│                             └──────────┘  │
└─────────────────────────────────────────────┘
```

### 5.4 撤销重做系统

#### 5.4.1 命令模式实现

```typescript
// 命令接口
interface Command {
  type: string;
  timestamp: number;
  
  execute(): void;
  undo(): void;
  redo(): void;
  
  // 合并同类命令 (如连续拖拽)
  merge(other: Command): Command | null;
}

// 示例: 移动节点命令
class MoveNodeCommand implements Command {
  type = 'move-node';
  
  constructor(
    private nodeId: string,
    private oldPosition: Point,
    private newPosition: Point
  ) {}
  
  execute() {
    setNodePosition(this.nodeId, this.newPosition);
  }
  
  undo() {
    setNodePosition(this.nodeId, this.oldPosition);
  }
  
  redo() {
    this.execute();
  }
  
  merge(other: Command): Command | null {
    if (other instanceof MoveNodeCommand && 
        other.nodeId === this.nodeId &&
        other.timestamp - this.timestamp < 500) {
      return new MoveNodeCommand(
        this.nodeId,
        this.oldPosition,
        other.newPosition
      );
    }
    return null;
  }
}
```

#### 5.4.2 历史栈管理

```typescript
class HistoryManager {
  private undoStack: Command[] = [];
  private redoStack: Command[] = [];
  private maxSize: number = 100;
  
  execute(command: Command) {
    command.execute();
    
    // 尝试合并到上一个命令
    const lastCommand = this.undoStack[this.undoStack.length - 1];
    if (lastCommand) {
      const merged = lastCommand.merge(command);
      if (merged) {
        this.undoStack[this.undoStack.length - 1] = merged;
        this.redoStack = []; // 清除 redo 栈
        return;
      }
    }
    
    this.undoStack.push(command);
    this.redoStack = [];
    
    // 限制栈大小
    if (this.undoStack.length > this.maxSize) {
      this.undoStack.shift();
    }
  }
  
  undo(): boolean {
    const command = this.undoStack.pop();
    if (!command) return false;
    
    command.undo();
    this.redoStack.push(command);
    return true;
  }
  
  redo(): boolean {
    const command = this.redoStack.pop();
    if (!command) return false;
    
    command.redo();
    this.undoStack.push(command);
    return true;
  }
}
```

---

## 6. 收益清算机制

### 6.1 上游资产识别

#### 6.1.1 引用图谱构建

```typescript
interface AssetReferenceGraph {
  root: string;              // 当前作品资产 ID
  
  nodes: {
    [assetId: string]: {
      type: 'asset' | 'skill' | 'template';
      metadata: AssetMetadata;
      rights: RightsConfiguration;
      royaltyRate: number;   // 要求的版税率
    };
  };
  
  edges: {
    from: string;
    to: string;
    relationship: 'direct' | 'indirect' | 'nested';
    usage: {
      type: 'input' | 'reference' | 'modification';
      percentage?: number;   // 使用量占比
    };
  }[];
}

// 递归构建引用图谱
async function buildReferenceGraph(
  blueprint: Blueprint,
  depth: number = 0,
  maxDepth: number = 10
): Promise<AssetReferenceGraph> {
  const graph: AssetReferenceGraph = {
    root: blueprint.id,
    nodes: {},
    edges: []
  };
  
  if (depth >= maxDepth) return graph; // 防止无限递归
  
  for (const node of blueprint.nodes) {
    if (node.type === 'asset') {
      const assetId = node.assetId;
      const asset = await fetchAsset(assetId);
      
      graph.nodes[assetId] = {
        type: 'asset',
        metadata: asset.metadata,
        rights: asset.rights,
        royaltyRate: asset.rights.derivative.royaltyRate
      };
      
      graph.edges.push({
        from: blueprint.id,
        to: assetId,
        relationship: 'direct',
        usage: { type: 'input' }
      });
      
      // 递归获取上游引用
      if (asset.blueprint) {
        const upstream = await buildReferenceGraph(asset.blueprint, depth + 1);
        Object.assign(graph.nodes, upstream.nodes);
        graph.edges.push(...upstream.edges.map(e => ({
          ...e,
          relationship: 'indirect' as const
        })));
      }
    } else if (node.type === 'skill') {
      const skillId = node.skillId;
      const skill = await fetchSkill(skillId);
      
      graph.nodes[skillId] = {
        type: 'skill',
        metadata: skill.metadata,
        rights: skill.rights,
        royaltyRate: skill.rights.usage.royaltyRate
      };
      
      graph.edges.push({
        from: blueprint.id,
        to: skillId,
        relationship: 'direct',
        usage: { type: 'reference' }
      });
    }
  }
  
  return graph;
}
```

#### 6.1.2 引用强度计算

```typescript
interface ReferenceStrength {
  assetId: string;
  directContribution: number;    // 直接贡献 (0-1)
  transitiveContribution: number; // 传递贡献
  totalContribution: number;      // 总贡献
}

function calculateReferenceStrength(
  graph: AssetReferenceGraph,
  rootId: string
): ReferenceStrength[] {
  const strengths: Map<string, ReferenceStrength> = new Map();
  
  // 初始化
  for (const assetId of Object.keys(graph.nodes)) {
    strengths.set(assetId, {
      assetId,
      directContribution: 0,
      transitiveContribution: 0,
      totalContribution: 0
    });
  }
  
  // 计算直接贡献
  for (const edge of graph.edges) {
    if (edge.relationship === 'direct') {
      const strength = strengths.get(edge.to)!;
      // 基于使用类型和百分比计算
      const baseWeight = edge.usage.type === 'input' ? 0.5 : 0.1;
      const usagePercent = edge.usage.percentage || 1;
      strength.directContribution += baseWeight * usagePercent;
    }
  }
  
  // 传递贡献衰减计算
  const decayFactor = 0.5; // 每层衰减 50%
  
  function calculateTransitive(nodeId: string, depth: number): number {
    if (depth > 10) return 0;
    
    const upstreamEdges = graph.edges.filter(e => e.to === nodeId);
    let transitive = 0;
    
    for (const edge of upstreamEdges) {
      const parentStrength = strengths.get(edge.from)!;
      transitive += parentStrength.directContribution * 
                    Math.pow(decayFactor, depth);
      transitive += calculateTransitive(edge.from, depth + 1);
    }
    
    return transitive;
  }
  
  // 计算每个资产的传递贡献
  for (const [assetId, strength] of strengths) {
    strength.transitiveContribution = calculateTransitive(assetId, 1);
    strength.totalContribution = Math.min(
      strength.directContribution + strength.transitiveContribution,
      1.0
    );
  }
  
  return Array.from(strengths.values());
}
```

### 6.2 分润比例计算

#### 6.2.1 分润算法

```typescript
interface RoyaltyDistribution {
  totalRevenue: number;
  platformFee: number;           // 平台服务费
  gasFee: number;               // 链上手续费
  creatorProceeds: number;      // 创作者净收益
  
  upstreamAllocations: {
    assetId: string;
    beneficiary: string;        // 收款地址
    amount: number;
    percentage: number;
    reason: string;            // 分配原因说明
  }[];
}

function calculateRoyaltyDistribution(
  salePrice: number,
  referenceGraph: AssetReferenceGraph,
  blueprintRights: RightsConfiguration
): RoyaltyDistribution {
  const PLATFORM_FEE_RATE = 0.10;  // 10% 平台费
  const GAS_FEE_ESTIMATE = 0.50;   // $0.50 预估 Gas
  
  const platformFee = salePrice * PLATFORM_FEE_RATE;
  const gasFee = GAS_FEE_ESTIMATE;
  const remaining = salePrice - platformFee - gasFee;
  
  // 计算上游总版税
  const upstreamAllocations: RoyaltyDistribution['upstreamAllocations'] = [];
  let totalUpstreamRoyalty = 0;
  
  for (const [assetId, node] of Object.entries(referenceGraph.nodes)) {
    // 基础版税率
    let royaltyRate = node.rights.derivative.royaltyRate || 0;
    
    // 基于引用强度调整
    const strength = calculateReferenceStrength(referenceGraph, assetId)
      .find(s => s.assetId === assetId);
    
    if (strength) {
      royaltyRate *= strength.totalContribution;
    }
    
    // 检查权利冲突
    if (node.rights.derivative.commercialUse === false && blueprintRights.commercialUse) {
      royaltyRate = 1; // 商业使用禁止时，收取 100% 作为补偿
    }
    
    const amount = remaining * royaltyRate;
    totalUpstreamRoyalty += amount;
    
    upstreamAllocations.push({
      assetId,
      beneficiary: node.metadata.creator,
      amount,
      percentage: royaltyRate * 100,
      reason: generateAllocationReason(node, strength)
    });
  }
  
  // 创作者净收益
  const creatorProceeds = remaining - totalUpstreamRoyalty;
  
  return {
    totalRevenue: salePrice,
    platformFee,
    gasFee,
    creatorProceeds,
    upstreamAllocations
  };
}
```

#### 6.2.2 冲突检测与解决

```typescript
interface RightsConflict {
  type: 'commercial_use' | 'modification_limit' | 'attribution_required' | 'exclusivity';
  severity: 'warning' | 'error' | 'blocking';
  assets: string[];
  description: string;
  resolution?: string;
}

function detectRightsConflicts(
  referenceGraph: AssetReferenceGraph,
  intendedRights: RightsConfiguration
): RightsConflict[] {
  const conflicts: RightsConflict[] = [];
  
  for (const [assetId, node] of Object.entries(referenceGraph.nodes)) {
    const upstreamRights = node.rights;
    
    // 商业使用冲突
    if (intendedRights.commercialUse && !upstreamRights.derivative.commercialUse) {
      conflicts.push({
        type: 'commercial_use',
        severity: 'error',
        assets: [assetId],
        description: `资产 ${node.metadata.name} 不允许商业使用`,
        resolution: '联系创作者获取商业授权，或更改作品为非商业用途'
      });
    }
    
    // 修改限制冲突
    const modificationLevels = ['none', 'light', 'moderate', 'heavy'];
    const intendedLevel = modificationLevels.indexOf(intendedRights.modificationScope);
    const allowedLevel = modificationLevels.indexOf(upstreamRights.derivative.modificationScope);
    
    if (intendedLevel > allowedLevel) {
      conflicts.push({
        type: 'modification_limit',
        severity: 'warning',
        assets: [assetId],
        description: `资产 ${node.metadata.name} 允许的修改程度为 ${upstreamRights.derivative.modificationScope}`,
        resolution: '减少对该资产的修改程度，或寻找替代资产'
      });
    }
    
    // 归属要求冲突
    if (upstreamRights.derivative.attributionRequired && !intendedRights.attribution) {
      conflicts.push({
        type: 'attribution_required',
        severity: 'error',
        assets: [assetId],
        description: `资产 ${node.metadata.name} 要求署名归属`,
        resolution: '在作品元数据中添加正确的归属信息'
      });
    }
    
    // 排他性冲突
    if (upstreamRights.derivative.exclusive) {
      conflicts.push({
        type: 'exclusivity',
        severity: 'warning',
        assets: [assetId],
        description: `资产 ${node.metadata.name} 具有排他性限制`,
        resolution: '了解排他性条款的具体限制范围'
      });
    }
  }
  
  return conflicts;
}
```

### 6.3 实时预览系统

#### 6.3.1 价格敏感度分析

```typescript
interface PriceSensitivityAnalysis {
  pricePoints: number[];         // 测试价格点
  distributions: RoyaltyDistribution[];
  
  insights: {
    optimalPriceRange: [number, number];  // 创作者收益最优价格区间
    breakEvenPrice: number;               // 盈亏平衡价格
    marginalReturns: number[];            // 边际收益变化
  };
}

function analyzePriceSensitivity(
  referenceGraph: AssetReferenceGraph,
  minPrice: number = 1,
  maxPrice: number = 1000,
  steps: number = 20
): PriceSensitivityAnalysis {
  const pricePoints: number[] = [];
  const distributions: RoyaltyDistribution[] = [];
  
  // 生成对数分布的价格点
  for (let i = 0; i < steps; i++) {
    const logMin = Math.log(minPrice);
    const logMax = Math.log(maxPrice);
    const price = Math.exp(logMin + (logMax - logMin) * (i / (steps - 1)));
    pricePoints.push(Math.round(price * 100) / 100);
  }
  
  for (const price of pricePoints) {
    distributions.push(
      calculateRoyaltyDistribution(price, referenceGraph, {} as RightsConfiguration)
    );
  }
  
  // 分析最优价格区间
  const creatorProceeds = distributions.map(d => d.creatorProceeds);
  const maxProceeds = Math.max(...creatorProceeds);
  const optimalStart = pricePoints[creatorProceeds.findIndex(p => p > maxProceeds * 0.9)];
  const optimalEnd = pricePoints[creatorProceeds.length - 1 - 
    [...creatorProceeds].reverse().findIndex(p => p > maxProceeds * 0.9)];
  
  return {
    pricePoints,
    distributions,
    insights: {
      optimalPriceRange: [optimalStart, optimalEnd],
      breakEvenPrice: pricePoints.find(p => 
        calculateRoyaltyDistribution(p, referenceGraph, {} as RightsConfiguration)
          .creatorProceeds > 0
      ) || minPrice,
      marginalReturns: pricePoints.map((p, i) => 
        i === 0 ? 0 : (distributions[i].creatorProceeds - distributions[i-1].creatorProceeds) / 
                      (p - pricePoints[i-1])
      )
    }
  };
}
```

### 6.4 智能合约集成

#### 6.4.1 收益分配合约

```solidity
// SPDX-License-Identifier: MIT
pragma solidity ^0.8.0;

contract DerivativeRoyaltyDistributor {
    struct Allocation {
        address beneficiary;
        uint256 percentage;     // 基点 (10000 = 100%)
        string assetId;
    }
    
    struct DerivativeBlueprint {
        string blueprintId;
        address creator;
        Allocation[] upstreamAllocations;
        uint256 platformFeePercentage;  // 基点
        bool isActive;
    }
    
    mapping(string => DerivativeBlueprint) public blueprints;
    mapping(string => uint256) public accumulatedRoyalties;
    
    event SaleDistributed(
        string indexed blueprintId,
        uint256 totalAmount,
        uint256 creatorAmount,
        uint256 platformAmount
    );
    
    function registerBlueprint(
        string memory _blueprintId,
        Allocation[] memory _allocations,
        uint256 _platformFee
    ) external {
        require(_platformFee <= 2000, "Platform fee too high"); // Max 20%
        
        uint256 totalPercentage = _platformFee;
        for (uint i = 0; i < _allocations.length; i++) {
            totalPercentage += _allocations[i].percentage;
        }
        require(totalPercentage <= 10000, "Total percentage exceeds 100%");
        
        DerivativeBlueprint storage blueprint = blueprints[_blueprintId];
        blueprint.blueprintId = _blueprintId;
        blueprint.creator = msg.sender;
        blueprint.platformFeePercentage = _platformFee;
        blueprint.isActive = true;
        
        for (uint i = 0; i < _allocations.length; i++) {
            blueprint.upstreamAllocations.push(_allocations[i]);
        }
    }
    
    function distributeSale(string memory _blueprintId) external payable {
        DerivativeBlueprint storage blueprint = blueprints[_blueprintId];
        require(blueprint.isActive, "Blueprint not active");
        require(msg.value > 0, "No payment received");
        
        uint256 remaining = msg.value;
        
        // 平台费用
        uint256 platformFee = (msg.value * blueprint.platformFeePercentage) / 10000;
        remaining -= platformFee;
        
        // 上游分配
        for (uint i = 0; i < blueprint.upstreamAllocations.length; i++) {
            Allocation memory alloc = blueprint.upstreamAllocations[i];
            uint256 amount = (msg.value * alloc.percentage) / 10000;
            remaining -= amount;
            
            (bool success, ) = alloc.beneficiary.call{value: amount}("");
            require(success, "Transfer to upstream failed");
            
            accumulatedRoyalties[alloc.assetId] += amount;
        }
        
        // 创作者收益
        (bool creatorSuccess, ) = blueprint.creator.call{value: remaining}("");
        require(creatorSuccess, "Transfer to creator failed");
        
        emit SaleDistributed(_blueprintId, msg.value, remaining, platformFee);
    }
}
```

---

## 7. 技术架构与实现

### 7.1 系统架构

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           前端应用层                                         │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │  React + TypeScript + WebGL                                          │  │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐   │  │
│  │  │ 画布引擎  │ │ 节点编辑  │ │ 属性面板  │ │ 预览渲染  │ │ 收益预览  │   │  │
│  │  │ (Fabric/ │ │(ReactFlow│ │(Formily) │ │(WebGL)   │ │(D3.js)   │   │  │
│  │  │  React   │ │ /xyflow) │ │          │ │          │ │          │   │  │
│  │  │  Flow)   │ │          │ │          │ │          │ │          │   │  │
│  │  └──────────┘ └──────────┘ └──────────┘ └──────────┘ └──────────┘   │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────────────┘
                                      │
                                      ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                           API 网关层                                         │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐ ┌──────────────┐       │
│  │ REST API     │ │ GraphQL      │ │ WebSocket    │ │ gRPC         │       │
│  │ (标准操作)   │ │ (复杂查询)   │ │ (实时推送)   │ │ (高性能调用) │       │
│  └──────────────┘ └──────────────┘ └──────────────┘ └──────────────┘       │
└─────────────────────────────────────────────────────────────────────────────┘
                                      │
                                      ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                           服务层                                             │
│  ┌────────────────┐ ┌────────────────┐ ┌────────────────┐ ┌──────────────┐  │
│  │ 编排服务       │ │ 执行服务       │ │ 权利服务       │ │ 资产服务     │  │
│  │ • 蓝图管理     │ │ • 任务调度     │ │ • 收益计算     │ │ • CRUD       │  │
│  │ • 版本控制     │ │ • 沙箱管理     │ │ • 冲突检测     │ │ • 搜索       │  │
│  │ • 协作编辑     │ │ • 缓存策略     │ │ • 合约交互     │ │ • 元数据     │  │
│  └────────────────┘ └────────────────┘ └────────────────┘ └──────────────┘  │
└─────────────────────────────────────────────────────────────────────────────┘
                                      │
                                      ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                           基础设施层                                         │
│  ┌────────────────┐ ┌────────────────┐ ┌────────────────┐ ┌──────────────┐  │
│  │ PostgreSQL     │ │ Redis          │ │ IPFS/Filecoin  │ │ ECHO Chain   │  │
│  │ (结构化数据)   │ │ (缓存/会话)    │ │ (文件存储)     │ │ (链上确权)   │  │
│  └────────────────┘ └────────────────┘ └────────────────┘ └──────────────┘  │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 7.2 关键技术选型

| 组件 | 技术选型 | 理由 |
|-----|---------|------|
| 前端框架 | React 18 + TypeScript | 类型安全，生态丰富 |
| 节点编辑器 | @xyflow/react (原 ReactFlow) | 功能完善，可定制性强 |
| 画布渲染 | HTML5 Canvas + WebGL | 性能优异，适合复杂图形 |
| 状态管理 | Zustand + Immer | 轻量，支持不可变更新 |
| 实时协作 | Yjs + WebSocket | CRDT 支持，冲突自由 |
| 后端服务 | Node.js + NestJS | 模块化，TypeScript 原生 |
| 执行引擎 | WebAssembly + Docker | 安全沙箱，环境隔离 |
| 区块链 | ECHO Protocol SDK | 原生支持，深度集成 |
| 存储 | PostgreSQL + Redis + IPFS | 分层存储，冷热分离 |

### 7.3 性能优化策略

#### 7.3.1 画布渲染优化

```typescript
// 虚拟渲染 - 只渲染视口内的节点
function VirtualizedCanvas({ nodes, viewport }: CanvasProps) {
  const visibleNodes = useMemo(() => {
    return nodes.filter(node => {
      const nodeBounds = getNodeBounds(node);
      return intersects(nodeBounds, viewport);
    });
  }, [nodes, viewport]);
  
  return (
    <Canvas>
      {visibleNodes.map(node => (
        <NodeComponent key={node.id} node={node} />
      ))}
    </Canvas>
  );
}

// 节点懒加载 - 复杂节点按需加载
function LazyNode({ node }: { node: Node }) {
  const [isLoaded, setIsLoaded] = useState(false);
  const [ref, inView] = useInView({ threshold: 0 });
  
  useEffect(() => {
    if (inView && !isLoaded) {
      setIsLoaded(true);
    }
  }, [inView]);
  
  return (
    <div ref={ref}>
      {isLoaded ? <ComplexNodeContent node={node} /> : <NodePlaceholder />}
    </div>
  );
}
```

#### 7.3.2 执行优化

```typescript
// 智能缓存 - 基于依赖图的最小化执行
class SmartExecutor {
  private cache: Map<string, ExecutionResult> = new Map();
  
  async execute(graph: NodeGraph, changedNodes: string[]): Promise<ExecutionResult> {
    // 构建依赖图
    const dependencyGraph = buildDependencyGraph(graph);
    
    // 找出需要重新执行的节点
    const affectedNodes = new Set<string>();
    for (const nodeId of changedNodes) {
      this.getAllDependents(dependencyGraph, nodeId, affectedNodes);
    }
    
    // 按拓扑排序执行
    const executionOrder = topologicalSort(graph.nodes, affectedNodes);
    
    for (const nodeId of executionOrder) {
      const cacheKey = this.generateCacheKey(nodeId, graph);
      
      if (this.cache.has(cacheKey) && !affectedNodes.has(nodeId)) {
        // 使用缓存结果
        continue;
      }
      
      // 执行节点
      const result = await this.executeNode(nodeId, graph);
      this.cache.set(cacheKey, result);
    }
    
    return this.collectResults(graph);
  }
}
```

---

## 8. 功能清单与工作估算

### 8.1 功能清单

#### Phase 1: MVP (最小可用产品)

| 模块 | 功能点 | 优先级 | 状态 |
|-----|--------|-------|------|
| **基础架构** | 项目创建与保存 | P0 | 待开发 |
| | 蓝图文件格式定义 | P0 | 待开发 |
| | 自动保存与版本历史 | P0 | 待开发 |
| **资产发现** | ECHO 资产搜索与筛选 | P0 | 待开发 |
| | 资产预览与授权检查 | P0 | 待开发 |
| | 资产导入到画布 | P0 | 待开发 |
| **可视化编排** | 节点拖拽与放置 | P0 | 待开发 |
| | 端口连接与验证 | P0 | 待开发 |
| | 基础节点类型 (资产/Skill/输出) | P0 | 待开发 |
| | 属性面板参数配置 | P0 | 待开发 |
| **执行引擎** | 本地沙箱执行 | P0 | 待开发 |
| | 基础缓存机制 | P0 | 待开发 |
| | 错误处理与日志 | P0 | 待开发 |
| **预览渲染** | 单节点预览 | P0 | 待开发 |
| | 完整链预览 | P0 | 待开发 |
| **发布确权** | 元数据填写 | P0 | 待开发 |
| | 基础权利配置 | P0 | 待开发 |
| | 链上提交 | P0 | 待开发 |

#### Phase 2: 增强功能

| 模块 | 功能点 | 优先级 | 状态 |
|-----|--------|-------|------|
| **资产发现** | 智能推荐引擎 | P1 | 待开发 |
| | 批量导入 | P1 | 待开发 |
| | 收藏夹与分类 | P1 | 待开发 |
| **可视化编排** | 节点分组与折叠 | P1 | 待开发 |
| | 撤销重做系统 | P1 | 待开发 |
| | 快捷键支持 | P1 | 待开发 |
| | 注释与标注 | P1 | 待开发 |
| **执行引擎** | 远程 Skill 调用 | P1 | 待开发 |
| | Web Worker 支持 | P1 | 待开发 |
| | 进度显示与取消 | P1 | 待开发 |
| **收益清算** | 上游资产识别 | P1 | 待开发 |
| | 分润计算与预览 | P1 | 待开发 |
| | 分配树可视化 | P1 | 待开发 |
| **协作功能** | 实时协作编辑 | P1 | 待开发 |
| | 评论与反馈 | P1 | 待开发 |

#### Phase 3: 高级功能

| 模块 | 功能点 | 优先级 | 状态 |
|-----|--------|-------|------|
| **高级编排** | 条件分支节点 | P2 | 待开发 |
| | 循环节点 | P2 | 待开发 |
| | 自定义脚本节点 | P2 | 待开发 |
| **AI 集成** | AI 辅助编排建议 | P2 | 待开发 |
| | 智能参数推荐 | P2 | 待开发 |
| | 自动化测试 | P2 | 待开发 |
| **模板系统** | 社区模板市场 | P2 | 待开发 |
| | 模板创建与分享 | P2 | 待开发 |
| | 模板收益分成 | P2 | 待开发 |
| **高级收益** | 冲突检测与解决 | P2 | 待开发 |
| | 价格敏感度分析 | P2 | 待开发 |
| | 批量收益结算 | P2 | 待开发 |
| **企业功能** | 私有 Skill 仓库 | P2 | 待开发 |
| | 团队权限管理 | P2 | 待开发 |
| | 审计日志 | P2 | 待开发 |

### 8.2 开发工作估算

#### 8.2.1 人力配置

| 角色 | 人数 | 职责 |
|-----|------|------|
| 产品经理 | 1 | 需求梳理、原型设计、用户反馈 |
| 前端工程师 | 2 | 可视化编排器、UI 组件、交互实现 |
| 后端工程师 | 2 | API 开发、执行引擎、区块链集成 |
| 智能合约工程师 | 1 | 收益分配合约、确权合约 |
| 测试工程师 | 1 | 自动化测试、性能测试、安全测试 |
| 设计师 | 1 | UI/UX 设计、图标、动效 |

#### 8.2.2 Phase 1: MVP 工作量估算 (6-8 周)

| 模块 | 功能点 | 估算人天 |
|-----|--------|---------|
| **项目管理** | 需求梳理、技术方案、排期 | 10 |
| **基础架构** | 项目结构、数据模型、API 定义 | 15 |
| **前端基础** | 布局框架、状态管理、组件库搭建 | 12 |
| **可视化编排器** | 节点系统、连线系统、画布引擎 | 20 |
| **资产发现模块** | 搜索 UI、资产卡片、导入功能 | 10 |
| **属性面板** | 参数表单、动态配置、验证 | 8 |
| **执行引擎** | 调度器、沙箱、缓存、错误处理 | 18 |
| **预览渲染** | 预览引擎、实时更新、进度显示 | 12 |
| **发布模块** | 元数据表单、链上交互、状态管理 | 10 |
| **集成测试** | 端到端测试、Bug 修复、优化 | 10 |
| **文档** | API 文档、用户指南 | 5 |
| **缓冲** | 不可预见工作 | 10 |
| **总计** | | **~140 人天 (7 周)** |

#### 8.2.3 Phase 2: 增强功能工作量估算 (6-8 周)

| 模块 | 功能点 | 估算人天 |
|-----|--------|---------|
| **高级编排功能** | 分组、撤销重做、快捷键 | 15 |
| **执行引擎增强** | 远程调用、Web Worker、流式处理 | 15 |
| **收益清算系统** | 上游识别、分润计算、可视化 | 18 |
| **智能推荐** | 推荐算法、个性化、A/B 测试 | 12 |
| **实时协作** | CRDT 集成、冲突解决、光标同步 | 15 |
| **性能优化** | 虚拟渲染、懒加载、缓存策略优化 | 10 |
| **测试与优化** | 性能测试、安全审计、Bug 修复 | 10 |
| **总计** | | **~95 人天 (5 周)** |

#### 8.2.4 Phase 3: 高级功能工作量估算 (8-10 周)

| 模块 | 功能点 | 估算人天 |
|-----|--------|---------|
| **高级节点类型** | 条件、循环、脚本节点 | 15 |
| **AI 功能** | 辅助建议、参数优化、自动化测试 | 18 |
| **模板系统** | 模板市场、创建工具、收益分成 | 12 |
| **高级收益功能** | 冲突检测、价格分析、批量结算 | 15 |
| **企业功能** | 权限管理、私有仓库、审计 | 15 |
| **移动端适配** | 响应式设计、触摸交互 | 10 |
| **国际化** | 多语言支持、本地化 | 8 |
| **测试与优化** | 全面测试、文档完善 | 10 |
| **总计** | | **~103 人天 (5-6 周)** |

#### 8.2.5 总工作量汇总

| 阶段 | 周期 | 人天 | 里程碑 |
|-----|------|------|--------|
| Phase 1 (MVP) | 7 周 | 140 | 可用内测版本 |
| Phase 2 (增强) | 5 周 | 95 | 公测版本 |
| Phase 3 (高级) | 6 周 | 103 | 正式版 v1.0 |
| **总计** | **~18 周 (4.5 个月)** | **~338 人天** | **完整产品** |

---

## 附录

### A. Skill 接口规范完整示例

```json
{
  "id": "echo.audio.enhancer.voice-clarity",
  "version": "2.1.0",
  "metadata": {
    "name": "语音清晰度增强",
    "description": "通过 AI 算法提升语音清晰度，去除背景噪音",
    "author": "did:echo:creator:audio_lab",
    "tags": ["audio", "enhancement", "voice", "ai"],
    "category": "audio_processing"
  },
  "interface": {
    "version": "1.0.0",
    "inputs": [
      {
        "id": "audio_input",
        "name": "输入音频",
        "type": "audio",
        "required": true,
        "description": "需要增强的音频文件",
        "constraints": {
          "maxDuration": 3600,
          "formats": ["wav", "mp3", "flac"]
        }
      }
    ],
    "outputs": [
      {
        "id": "enhanced_audio",
        "name": "增强音频",
        "type": "audio",
        "description": "增强后的音频"
      },
      {
        "id": "processing_report",
        "name": "处理报告",
        "type": "json",
        "description": "包含噪声降低比例、清晰度提升等数据"
      }
    ],
    "parameters": [
      {
        "id": "noise_reduction",
        "name": "降噪强度",
        "type": "number",
        "default": 0.7,
        "description": "背景噪音降低程度",
        "constraints": {
          "min": 0,
          "max": 1,
          "step": 0.1
        },
        "ui": {
          "widget": "slider",
          "group": "基本设置",
          "order": 1
        }
      },
      {
        "id": "clarity_boost",
        "name": "清晰度增强",
        "type": "number",
        "default": 0.5,
        "constraints": {
          "min": 0,
          "max": 1,
          "step": 0.1
        },
        "ui": {
          "widget": "slider",
          "group": "基本设置",
          "order": 2
        }
      },
      {
        "id": "output_format",
        "name": "输出格式",
        "type": "string",
        "default": "wav",
        "constraints": {
          "options": ["wav", "mp3", "flac"]
        },
        "ui": {
          "widget": "dropdown",
          "group": "高级设置",
          "order": 1
        }
      }
    ]
  },
  "execution": {
    "runtime": "remote",
    "resources": {
      "memory": 2048,
      "cpu": 2,
      "gpu": true
    },
    "timeout": 300000,
    "retryPolicy": {
      "maxAttempts": 3,
      "backoff": "exponential"
    }
  },
  "rights": {
    "usage": {
      "price": 0.5,
      "currency": "USD"
    },
    "derivative": {
      "allowed": true,
      "royaltyRate": 0.05,
      "commercialUse": true,
      "attributionRequired": true
    }
  }
}
```

### B. Blueprint 文件格式规范

```json
{
  "version": "1.0.0",
  "metadata": {
    "name": "Lo-fi 混音生成器",
    "description": "自动将输入音频转换为 Lo-fi 风格",
    "creator": "did:echo:creator:beatmaker_01",
    "createdAt": "2026-04-19T10:00:00Z",
    "updatedAt": "2026-04-19T12:30:00Z",
    "tags": ["lo-fi", "remix", "audio"]
  },
  "canvas": {
    "viewport": {
      "x": 0,
      "y": 0,
      "zoom": 1
    }
  },
  "nodes": [
    {
      "id": "asset-1",
      "type": "asset",
      "assetId": "echo://asset/audio/input_track_123",
      "display": {
        "position": { "x": 100, "y": 200 },
        "size": { "width": 180, "height": 80 }
      }
    },
    {
      "id": "skill-1",
      "type": "skill",
      "skillId": "echo.audio.effect.lo-fi-filter",
      "parameters": {
        "intensity": 0.8,
        "vinyl_noise": true
      },
      "display": {
        "position": { "x": 400, "y": 200 }
      }
    },
    {
      "id": "skill-2",
      "type": "skill",
      "skillId": "echo.audio.effect.reverb",
      "parameters": {
        "room_size": 0.6,
        "damping": 0.3
      },
      "display": {
        "position": { "x": 700, "y": 200 }
      }
    },
    {
      "id": "output-1",
      "type": "output",
      "export": {
        "format": "wav",
        "quality": "high"
      },
      "display": {
        "position": { "x": 1000, "y": 200 }
      }
    }
  ],
  "connections": [
    {
      "id": "conn-1",
      "source": { "nodeId": "asset-1", "portId": "output" },
      "target": { "nodeId": "skill-1", "portId": "audio_input" }
    },
    {
      "id": "conn-2",
      "source": { "nodeId": "skill-1", "portId": "processed_audio" },
      "target": { "nodeId": "skill-2", "portId": "audio_input" }
    },
    {
      "id": "conn-3",
      "source": { "nodeId": "skill-2", "portId": "processed_audio" },
      "target": { "nodeId": "output-1", "portId": "input" }
    }
  ],
  "rights": {
    "copy": { "allowed": true },
    "commercialUse": { "allowed": true, "fee": 0.1 },
    "modification": { "allowed": true, "scope": "heavy" },
    "derivative": { 
      "allowed": true, 
      "royaltyRate": 0.08,
      "attributionRequired": true
    }
  }
}
```

---

*文档结束*

> **设计参考**: Ableton Live (音频工作流), DaVinci Resolve/Fusion (节点编辑), ComfyUI (AI 工作流), n8n (自动化工作流), Figma (协作体验)

> **作者**: ECHO Product Design Team  
> **最后更新**: 2026-04-19