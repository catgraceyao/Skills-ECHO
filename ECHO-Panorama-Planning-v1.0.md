# ECHO 原生分布式价值网络 — 全景配套规划 v1.0

> **文档定位**: 与 ECHO 协议设计相匹配的完整配套设施规划
> **配套范围**: 从主权边界到用户界面，从链上合约到运营工具
> **核心原则**: 势是描述框架，配套设施是观测工具；四权是创作者宪法，配套设施是宪法执行系统
> **规划日期**: 2026-04-21

---

## 目录

1. [配套哲学：不是控制系统，是边界设施](#一配套哲学)
2. [六层配套架构总览](#二六层配套架构总览)
3. [Layer 0: 链上主权协议设施](#三layer-0-链上主权协议设施)
4. [Layer 1: 沙箱主权边界设施](#四layer-1-沙箱主权边界设施)
5. [Layer 2: 生命状态计算设施](#五layer-2-生命状态计算设施)
6. [Layer 3: 价值流动与验证设施](#六layer-3-价值流动与验证设施)
7. [Layer 4: 发现与编排设施](#七layer-4-发现与编排设施)
8. [Layer 5: 创作者赋能设施](#八layer-5-创作者赋能设施)
9. [跨层协作流程](#九跨层协作流程)
10. [实施路线图](#十实施路线图)
11. [附录：配套与核心概念的映射](#十一附录)

---

## 一、配套哲学

### 1.1 核心命题

> **"ECHO 不是 Skill 的房东，而是国境线的划定者和检查站。"**

配套设施的设计必须遵循同一命题：
- ❌ 不是平台控制创作者
- ✅ 是协议边界保护创作者主权
- ❌ 不是系统决定资产价值
- ✅ 是市场通过使用行为涌现价值
- ❌ 不是排名算法推荐内容
- ✅ 是关系网络自然发现关联

### 1.2 配套的三种角色

```
┌─────────────────────────────────────────────────────────────┐
│                     配套设施的三重角色                        │
├─────────────────┬─────────────────┬─────────────────────────┤
│   边界守卫者     │    状态记录员    │      价值结算员         │
│  (主权边界设施)   │  (生命状态设施)   │    (价值流动设施)       │
├─────────────────┼─────────────────┼─────────────────────────┤
│ • 沙箱隔离       │ • ShiGraph计算   │ • 收益自动分配          │
│ • ZKP验证        │ • 势场测绘       │ • PoU验证              │
│ • 权限校验       │ • 变势预警       │ • 级联清算             │
│ • 隐私保护       │ • 关系网络       │ • 收益权交易           │
└─────────────────┴─────────────────┴─────────────────────────┘
```

### 1.3 设计红线

- **不替代思考**: 系统建议仅供参考，最终决定权在创作者
- **不包装去中心化**: 承认中心化要素（官方 Indexer、团队0.1%），但逐步放权
- **不追求完美**: BA模式（Best Attempt），先让系统跑起来
- **每一层都有价值**: L1-L5 每一层都能独立使用
- **尊重创作者主权**: 四权配置是创作者的"宪法"

---

## 二、六层配套架构总览

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                                                                             │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │  Layer 5: 创作者赋能层 (Creator Empowerment Layer)                  │   │
│   │                                                                     │   │
│   │  ┌────────────┐  ┌────────────┐  ┌────────────┐  ┌────────────┐    │   │
│   │  │  Blueprint │  │   Asset    │  │  Revenue   │  │  Alert     │    │   │
│   │  │   Studio   │  │ Dashboard  │  │  Manager   │  │  Center    │    │   │
│   │  │ (蓝图编辑)  │  │ (资产看板)  │  │ (收益管理)  │  │ (预警中心)  │    │   │
│   │  └────────────┘  └────────────┘  └────────────┘  └────────────┘    │   │
│   │                                                                     │   │
│   │  面向创作者：配置四权、观测势位、管理收益、接收预警                  │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│                                    ▲                                        │
│                                    │                                        │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │  Layer 4: 发现与编排层 (Discovery & Orchestration Layer)           │   │
│   │                                                                     │   │
│   │  ┌────────────┐  ┌────────────┐  ┌────────────┐  ┌────────────┐    │   │
│   │  │  Relation  │  │   Agent    │  │   Skill    │  │   User     │    │   │
│   │  │  Explorer  │  │  Router    │  │  Market    │  │  Wallet    │    │   │
│   │  │(关系网络图) │  │(编排引擎)   │  │(发现市场)   │  │(用户钱包)   │    │   │
│   │  └────────────┘  └────────────┘  └────────────┘  └────────────┘    │   │
│   │                                                                     │   │
│   │  面向用户：发现Skill、编排工作流、管理余额、提交争议                │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│                                    ▲                                        │
│                                    │                                        │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │  Layer 3: 价值流动与验证层 (Value Flow & Verification Layer)       │   │
│   │                                                                     │   │
│   │  ┌────────────┐  ┌────────────┐  ┌────────────┐  ┌────────────┐    │   │
│   │  │   PoU      │  │  Revenue   │  │ Validator  │  │  Dispute   │    │   │
│   │  │  Engine    │  │  Splitter  │  │   Node     │  │  Arbiter   │    │   │
│   │  │(使用证明)   │  │(收益分配)   │  │(验证节点)   │  │(争议仲裁)   │    │   │
│   │  └────────────┘  └────────────┘  └────────────┘  └────────────┘    │   │
│   │                                                                     │   │
│   │  面向网络：验证使用、分配收益、维护共识、解决争议                    │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│                                    ▲                                        │
│                                    │                                        │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │  Layer 2: 生命状态计算层 (Life State Computation Layer)            │   │
│   │                                                                     │   │
│   │  ┌────────────┐  ┌────────────┐  ┌────────────┐  ┌────────────┐    │   │
│   │  │ ShiGraph   │  │  ShiField  │  │   Shi      │  │  Anomaly   │    │   │
│   │  │   Engine   │  │  Mapper    │  │  Watcher   │  │  Detector  │    │   │
│   │  │(三维编织)   │  │(势场测绘)   │  │(变势观测)   │  │(异常检测)   │    │   │
│   │  └────────────┘  └────────────┘  └────────────┘  └────────────┘    │   │
│   │                                                                     │   │
│   │  面向系统：从事件流计算生命状态、测绘势场、观测变势、检测异常      │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│                                    ▲                                        │
│                                    │                                        │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │  Layer 1: 沙箱主权边界层 (Sandbox Sovereignty Boundary Layer)       │   │
│   │                                                                     │   │
│   │  ┌────────────┐  ┌────────────┐  ┌────────────┐  ┌────────────┐    │   │
│   │  │   Entry    │  │  Creator   │  │ Settlement │  │   ZKP      │    │   │
│   │  │  Sandbox   │  │  Sandbox   │  │  Sandbox   │  │  Generator │    │   │
│   │  │ (入口沙箱)  │  │ (创作者私域)│  │ (结算沙箱)  │  │(零知识证明)  │    │   │
│   │  └────────────┘  └────────────┘  └────────────┘  └────────────┘    │   │
│   │                                                                     │   │
│   │  面向执行：验证权限、私域执行、结算分配、生成证明                    │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│                                    ▲                                        │
│                                    │                                        │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │  Layer 0: 链上主权协议层 (On-Chain Sovereignty Protocol Layer)     │   │
│   │                                                                     │   │
│   │  ┌────────────┐  ┌────────────┐  ┌────────────┐  ┌────────────┐    │   │
│   │  │  ShiShu    │  │  ShiField  │  │   Policy   │  │  Revenue   │    │   │
│   │  │  Registry  │  │  Registry  │  │   Engine   │  │  Contract  │    │   │
│   │  │(势枢注册表) │  │(势场注册表) │  │(策略引擎)   │  │(收益合约)   │    │   │
│   │  └────────────┘  └────────────┘  └────────────┘  └────────────┘    │   │
│   │                                                                     │   │
│   │  面向共识：存储三维坐标、记录势场、计算有效配置、执行自动分润      │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 三、Layer 0: 链上主权协议设施

### 3.1 核心合约矩阵

| 合约 | 职责 | 核心数据结构 | 关键事件 |
|------|------|-------------|---------|
| **EchoCore** | 资产登记、四权配置存储 | `Asset {owner, contentHash, blueprintHash, createdAt}` | `AssetRegistered` |
| **ShiShuRegistry** | 三维坐标存储、势场边界更新 | `ShiShuCoordinate {timeLevel, spaceLevel, relationLevel}` | `CoordinateRecorded` |
| **ShiPositionRegistry** | 六爻坐标、势能、跃迁历史 | `ShiPosition {timeYao, spaceYao, relationYao, shiEnergy}` | `PositionUpdated` |
| **ShiFieldRegistry** | 64势场区域、周期Merkle根 | `ShiFieldEpoch {timestamp, merkleRoot, regions[64]}` | `EpochCreated` |
| **PolicyEngine** | 有效配置计算、冲突检测 | `EffectivePolicy {yong, yan, kuo, yi, conflicts[]}` | `PolicyAdjusted` |
| **RevenueSplitter** | 实时分润、级联清算 | `RevenueDistribution {recipients[], shares[]}` | `RevenueDistributed` |

### 3.2 四权配置合约

```solidity
struct Blueprint {
    uint8 yong;      // 用权: 0-5 (私密→完全开放)
    uint8 yan;       // 衍权: 0-5 (禁止→完全开放)
    uint8 kuo;       // 扩权: 0-5 (锁定→全网发现)
    uint8 yi;        // 益权: 0-5 (免费→最大化收益)
    uint256 version; // 版本号（软分叉支持）
    bytes32 contentHash; // 配置哈希
}

struct UseConfig {
    PricingModel model;    // 定价模型
    uint256 basePrice;     // 基础价格
    UsageScope scope;      // 使用范围
    uint256 maxUsage;      // 最大使用次数
    bool transferable;     // 是否可转售
}

struct DerConfig {
    bool allowed;          // 是否允许衍生
    uint256 upstreamShare; // 上游分润比例
    uint256 maxDepth;      // 最大衍生层级
    DerType[] allowedTypes;// 允许的衍生类型
}
```

### 3.3 权利相容机制

```
创作者意图（Blueprint） ∩ 物理法则范围（HexagramRules） = 实际生效配置

如果 创作者配置 ⊂ 物理法则范围：完全尊重创作者意图
如果 创作者配置 ⊄ 物理法则范围：强制调整到交集

冲突严重程度：
- distance ≥ 2 → BLOCKING（强制调整）
- distance = 1  → CRITICAL（预警+协商）
- distance = 0  → WARNING（提示建议）
```

### 3.4 经济合约

| 合约 | 功能 | 关键参数 |
|------|------|----------|
| **ECHOToken** | DCEP 1:1映射代币 | `mint(address, amount)` `burn(address, amount)` |
| **Escrow** | 托管账户管理 | 银行合作/第三方支付 |
| **Staking** | 验证者质押 | `stake(amount)` `unstake(amount, lockPeriod)` |
| **Slashing** | 罚没机制 | `slash(validator, reason, percentage)` |
| **BenefitMarket** | 收益权交易 | `splitShares(assetId, percentage, price)` |

---

## 四、Layer 1: 沙箱主权边界设施

### 4.1 三层沙箱架构

```
┌─────────────────────────────────────────────────────────────────┐
│                        用户发起使用请求                          │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│  Layer 1a: 入口沙箱 (ECHO控制)                                   │
│  ┌─────────────────────────────────────────────────────────────┐ │
│  │ • 验证用户余额                                              │ │
│  │ • 预扣费用（本次调用预估费用）                               │ │
│  │ • 检查「用」权配置（是否允许该用户调用）                      │ │
│  │ • 生成调用凭证（含时间戳、nonce）                             │ │
│  └─────────────────────────────────────────────────────────────┘ │
│  边界：ECHO完全控制，用户透明                                   │
└─────────────────────────────────────────────────────────────────┘
                              │ 提交凭证
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│  Layer 1b: 创作者沙箱 (创作者主权)                              │
│  ┌─────────────────────────────────────────────────────────────┐ │
│  │ • 接收调用凭证                                               │ │
│  │ • 执行Skill核心逻辑（私有模型、代码）                          │ │
│  │ • 记录使用数据（时长、资源消耗、质量评分）                      │ │
│  │ • 生成ZKP证明（证明"我执行了X秒，消耗Y资源"）                │ │
│  │ • 返回结果给用户                                              │ │
│  └─────────────────────────────────────────────────────────────┘ │
│  边界：ECHO永远不能进入此区域                                     │
│  主权：创作者拥有完全控制权                                       │
└─────────────────────────────────────────────────────────────────┘
                              │ 提交ZKP证明
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│  Layer 1c: 结算沙箱 (ECHO控制)                                   │
│  ┌─────────────────────────────────────────────────────────────┐ │
│  │ • 验证ZKP证明有效性                                           │ │
│  │ • 确认执行确实发生                                            │ │
│  │ • 实际扣费（从预扣到实际结算）                                 │ │
│  │ • 按四权分配收益（创作者+上游+验证者+协议）                   │ │
│  │ • 更新链上状态（使用次数+1、收益记录）                         │ │
│  │ • 生成PoU事件提交到事件流                                      │ │
│  └─────────────────────────────────────────────────────────────┘ │
│  边界：ECHO完全控制，验证但不窥视                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 4.2 隔离级别矩阵

| 级别 | 技术方案 | 适用场景 | 启动速度 | 安全性 |
|------|----------|----------|----------|--------|
| **L1** | WASM Runtime | 简单脚本、快速启动 | <100ms | 进程隔离 |
| **L2** | gVisor Container | 中等复杂度、需要系统调用 | 1-2s | 系统调用过滤 |
| **L3** | Kata Containers | 重型AI模型、最高安全需求 | 3-5s | 硬件级VM隔离 |

### 4.3 ZKP生成设施

```
创作者沙箱内执行 → 生成ZKP证明

证明内容：
- 执行确实发生（不暴露执行内容）
- 资源消耗（CPU时间、内存用量）
- 满足四权配置（用户有权调用）

技术选型（待决策）：
- Risc0: Rust生态，证明生成快
- SP1: 新方案，可能更优
- zk-SNARK: 成熟但设置复杂

成本承担（待决策）：
- 平台补贴（长期负担）
- 调用方支付（用户成本）
- 创作者承担（门槛提高）
```

### 4.4 异步结算队列

```
高并发场景下的处理流程：

用户请求 → 入口沙箱验证 → 创作者沙箱执行（立即返回结果）
                                              ↓
                                        异步结算队列
                                              ↓
                                    结算沙箱（后台处理）
                                              ↓
                                        链上确认

优势：用户体验好（延迟<1秒）
挑战：需要处理失败回滚（结算失败时补偿用户）
```

---

## 五、Layer 2: 生命状态计算设施

### 5.1 ShiGraph计算引擎

**核心命题**: ShiGraph 不是存储结构，是计算投影

```
ShiGraph = fold(EventStream, ShiProjection)

输入: Kafka事件流（过去90天）
输出: 三维坐标 [时间, 空间, 关系] + 六爻坐标 + 势能度量

计算频率（待决策）：
- 方案A: 每次使用实时计算（精准但成本高，100ms延迟）
- 方案B: 每小时批量计算（成本低但延迟高）
- 方案C: 混合（实时更新累计指标，每小时映射到64卦）← 推荐
```

### 5.2 三维维度计算

```python
def calculate_shigraph(asset_id):
    # 1. 读取事件流
    events = event_store.query(asset_id, since=now()-90_days)
    
    # 2. 时间维度
    usage_7d = count(events.filter(type=Usage, since=7_days))
    usage_30d = count(events.filter(type=Usage, since=30_days))
    temporal_score = calculate_temporal(usage_7d, usage_30d, trend)
    # 输出: 0(沉寂) | 1(稳定) | 2(活跃) | 3(爆发)
    
    # 3. 空间维度
    sandboxes = unique(events.filter(type=Deployment).sandbox_id)
    spatial_score = calculate_spatial(len(sandboxes), geo_distribution)
    # 输出: 0(单沙箱) | 1(少沙箱) | 2(多沙箱) | 3(跨域)
    
    # 4. 关系维度
    citations = count(events.filter(type=Citation))
    derivatives = count(events.filter(type=Derivative))
    dependents = count_dependent_assets(asset_id)
    relational_score = calculate_relational(citations, derivatives, dependents)
    # 输出: 0(孤立) | 1(弱连接) | 2(中连接) | 3(强生态)
    
    # 5. 组合三维坐标
    coordinate = [temporal_score, spatial_score, relational_score]
    
    # 6. 映射到状态
    state = map_coordinate_to_state(coordinate)
    # 例如: [2,2,3] → "咸卦·大成期" 或 Stage.MATURE
    
    return {
        "coordinate": coordinate,
        "state": state,
        "metrics": {usage_7d, sandboxes, derivatives}
    }
```

### 5.3 势场测绘系统（每周执行）

```
Step 1: 数据采集
  └── 获取全网资产4维配置（用/扩/衍/益得分）

Step 2: 降维聚类
  ├── t-SNE降至2D平面
  ├── DBSCAN聚类（eps=3.5, min_samples=5）
  └── 输出: 自然聚集区域

Step 3: 特征描述
  ├── 计算每个区域的中心特征
  ├── 匹配传统卦名（文化命名）
  └── 记录: "本周观测到的高开放高衍生型区域 → 姤卦"

Step 4: 链上提交
  ├── 计算Merkle Root（所有资产-区域映射）
  ├── 生成ZK proof（证明计算正确性）
  └── ShiFieldRegistry.createEpoch(merkleRoot, regions)
```

### 5.4 变势预警系统

```
三级预警时间线：

第一级（50%阈值）—— 教育预警
└── "您的资产正在快速成长，了解下一生长阶段"
    └── 展示: 什么是成长期？有什么特征？

第二级（80%阈值）—— 调整预警
└── "3天后配置将自动调整"
    └── 展示: 变化预览、物理法则解释、休眠选项
    └── 选择: 接受 / 手动调整（在范围内） / 申请休眠

第三级（100%阈值）—— 变势生效
└── 配置调整生效
    └── 但创作者已提前知晓并同意（或选择休眠）

休眠规则：
- 每年最多3次
- 每次最长90天
- 期间势位锁定，约束不生效
- 已授权用户权益不受影响
```

### 5.5 异常检测系统

| 检测类型 | 指标 | Z-Score阈值 | 可能原因 |
|----------|------|-------------|----------|
| **时间爆发** | 7天使用量突增 | >3 | 病毒传播、刷量、活动推广 |
| **空间跳跃** | 平台数突增 | >3 | 突然多平台分发 |
| **关系集群** | 引用/衍生数突增 | >3 | 互刷引用、合作创作 |
| **新势场现象** | 全新聚集模式 | >4 | 全新传播模式、测量误差 |

**处理原则**: 不是惩罚，是记录。"这里有些不同寻常，值得关注。"

---

## 六、Layer 3: 价值流动与验证设施

### 6.1 PoU（使用证明）系统

```
用户使用Skill
    ↓
沙箱执行并计量（时长、资源、质量）
    ↓
生成PoU证明：
{
    event_id: "evt_001",
    type: "UsageOccurred",
    asset_id: "skill_001",
    user_id: "user_123",
    timestamp: "2026-03-28T10:00:00Z",
    context: {
        task: "write_poem",
        quality_score: 0.95,
        duration: 1500
    },
    cost: 0.01,
    proof: {
        type: "zk-SNARK",      // MVP可选，先普通签名
        public_inputs: {...},
        proof_data: "..."
    },
    signatures: {
        sandbox: "sig_abc",    // 沙箱签名
        user: "sig_def"        // 用户签名
    }
}
    ↓
提交验证节点
    ↓
验证者验证（沙箱签名有效性、数据合理性）
    ↓
7天挑战期（任何人可质疑）
    ↓
无争议 → 确认上链 → 触发ShiGraph更新
有争议 → 仲裁处理
```

### 6.2 验证者网络

**验证者权重公式**:
```
Weight = 50% × 使用历史得分 + 30% × 质押金额 + 10% × 验证准确率 + 10% × 活跃度
```

**验证者设施**:
- 验证节点客户端（轻节点/全节点）
- 质押管理界面（质押/解锁/收益查看）
- 验证历史查询（准确率统计）
- 争议处理界面（响应质疑）

**罚没机制**:
- 验证错误: 罚没10-50%
- 恶意伪造: 罚没100%
- 长期离线: 罚没5-20%

### 6.3 收益分配引擎

**单次使用分配**:
```
用户支付 1 ECHO
├── 创作者: 0.99 (99%)
├── 验证者: 0.009 (0.9%，按权重分配)
└── ECHO协议: 0.001 (0.1%)
```

**衍生作品级联分配**:
```
Skill B（衍生）调用 Skill A（原创）
用户支付 20 ECHO 使用 B

分配:
├── B创作者: 17.00 (85%)
├── A创作者:  2.00 (10%，由衍权规则触发)
├── 验证者:   0.90 (4.5%)
└── 协议:     0.10 (0.5%)

如果 B 还调用 C:
├── B创作者: 14.00 (70%)
├── A创作者:  2.00 (10%)
├── C创作者:  3.00 (15%)
├── 验证者:   0.90 (4.5%)
└── 协议:     0.10 (0.5%)
```

**收益权市场**:
- 创作者出售未来收益的30%给投资者
- 投资者类型: 现金流型/增长型/影响力型
- 分层解锁: 冷启动期锁定 → 成熟期开放交易

### 6.4 争议仲裁系统

**争议类型**:
- PoU真实性（使用是否伪造）
- 四权纠纷（配置解释冲突）
- 收益分配（分配比例争议）
- 质量纠纷（Skill质量不达标）

**仲裁流程**:
```
争议提交 → 初步审查（形式审查） → 证据收集（7天）
    ↓
仲裁决策（验证者委员会投票） → 执行公示（链上记录）
```

---

## 七、Layer 4: 发现与编排设施

### 7.1 关系网络发现系统

**终极目标**:
```
无预设分类
Skill功能由"它和谁连接"定义
完全涌现，去中心化
```

**渐进路径**:
```
Phase 1 (启动期): 语义标签 + 协同过滤
  └── 功能标签[文本生成][图像处理] + "用过A的人也用过B"

Phase 2 (成长期): 关系网络涌现
  └── 依赖关系 + 扩展关系 + 对话关系

Phase 3 (成熟期): 纯关系网络
  └── 无分类、无标签、完全由使用行为定义
```

**关系类型**:
```
⬇️ 依赖关系: C和D都用A的分析结果 → C依赖A
➡️ 扩展关系: B在A基础上加功能 → B扩展A
↔️ 对话关系: A和B互相调用 → 对话关系
```

### 7.2 Agent编排引擎

**编排流程**:
```
用户说: "帮我做一份腾讯Q3财报深度解读"

Step 1: 意图理解
  └── [任务类型=财报分析, 目标公司=腾讯, 时间=Q3]

Step 2: 任务分解
  └── 获取PDF → 提取指标 → 可视化 → 撰写结论

Step 3: Skill查询（去关系网络问）
  └── "谁能解析PDF?" → 返回3个候选
  └── "谁能提取财报指标?" → 返回5个候选
  └── "谁能做可视化?" → 返回4个候选

Step 4: Skill选择（多目标优化）
  └── 功能匹配度 × 历史成功率 × 响应时间 × 成本

Step 5: 序列编排（依赖解析）
  └── S1(解析PDF) → S2(提取指标) → (S3∥S4)(可视化+结论)

Step 6: 执行监控（故障处理）
  └── S3失败 → 启动降级: 换备用Skill 或 跳过图表
```

**事务模式**:
| 模式 | 场景 | 处理 |
|------|------|------|
| **全有或全无** | 金融交易 | 任一失败全部回滚 |
| **部分成功** | 批量分析 | 记录成功，失败重试或跳过 |
| **补偿机制** | 跨平台操作 | 失败时执行补偿（如退款） |

### 7.3 语义标准化设施

**每个Skill必须提供**:
```json
{
  "function_tags": ["文本生成", "图像处理"],
  "input_schema": { /* JSON Schema */ },
  "output_schema": { /* JSON Schema */ },
  "capability_boundary": "能做什么，不能做什么",
  "quality_metrics": {
    "accuracy": 0.95,
    "response_time_ms": 1200,
    "compute_units": 512
  }
}
```

**向量化表示**:
- 文本向量化（BERT/Word2Vec）
- 功能图谱嵌入
- 用户行为协同过滤

### 7.4 用户钱包设施

**功能**:
- ECHO余额管理（DCEP 1:1映射）
- 使用授权（预付卡模式：先购买授权，后随时使用）
- 使用历史查询
- 争议提交

---

## 八、Layer 5: 创作者赋能设施

### 8.1 Blueprint Studio（四权蓝图编辑器）

**定位**: "Figma 遇见了 Uniswap，为数字资产的权利配置而生"

**核心功能**:
- 可视化四权配置编辑器（用/衍/扩/益）
- 实时预览: 生效配置 vs 创作者意图
- 版本管理（软分叉支持）
- 模板库（5种活法模板）

**渐进引导系统（L1-L4）**:
```
L1 (30秒): 选模板
  └── "私房工具" [0,0,0,0] / "手艺人" [2,0,2,1] / "基础设施" [2,2,2,2]

L2 (2分钟): 回答4个场景问题
  └── 你想获得什么？希望多少人用？别人能改吗？怎么收钱？

L3 (5分钟): 看后果预览
  └── "如果扩=2，预计发现量+300%，但竞争加剧"

L4 (高级): 完全手动
  └── 每个维度0-5精细调整，查看物理法则约束范围
```

**后果预览示例**:
```
当前配置: [2,0,2,1]（手艺人）

如果改为 [2,1,2,2]:
├── 衍权开放 → 预计衍生作品: +2个/月
├── 益权解锁 → 被动收入: +15 ECHO/月
├── 竞争风险: 中等（核心算法仍保护）
└── 生态健康度: 提升（从孤立到连接）
```

### 8.2 Asset Dashboard（资产看板）

**主界面**:
```
┌─────────────────────────────────────────────────────────────────────┐
│  资产: 智能写诗助手 (skill_poet_alice)              [分享] [设置]  │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  当前势位                                                    │   │
│  │  ┌────────┐  ┌────────┐  ┌────────┐                        │   │
│  │  │  咸卦   │  │ 大成期  │  │ 1,247气数│                        │   │
│  │  └────────┘  └────────┘  └────────┘                        │   │
│  │  六爻坐标: (九四, 九二, 九三)                               │   │
│  │  三维: [时间:2活跃] [空间:1少平台] [关系:2网络]              │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                      │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐              │
│  │   时间维度    │  │   空间维度    │  │   关系维度    │              │
│  │   ████████   │  │   ████░░░░   │  │   █████░░░   │              │
│  │   280次/周   │  │   2个沙箱    │  │   12次引用   │              │
│  │   趋势 ↑23%  │  │   新增 +1    │  │   新增 +3    │              │
│  └──────────────┘  └──────────────┘  └──────────────┘              │
│                                                                      │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  关系网络                                                    │   │
│  │  ○ 营销文案助手 (衍生) ──→ 本月收益: 0.003 ECHO            │   │
│  │  ○ 古文翻译工具 (引用) ──→ 引用次数: 3                     │   │
│  │  ○ 诗歌教学助手 (引用) ──→ 引用次数: 5                     │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                      │
│  本月收益: 12.5 ECHO    累计使用: 1,247次    预警: 无              │
│                                                                      │
└─────────────────────────────────────────────────────────────────────┘
```

### 8.3 Alert Center（预警中心）

**预警类型**:
- **变势预警**: "3天后进入大成期，衍权将强制调整为≥3"
- **收益异常**: "今日收益突增300%，可能原因：病毒传播"
- **关系变化**: "新增3个衍生作品，生态扩展中"
- **配置冲突**: "当前配置与物理法则有1处冲突，建议调整"

**响应选项**:
- 接受（系统自动调整）
- 手动调整（在物理法则范围内）
- 申请休眠（暂停约束，最长90天）
- 忽略（了解风险，保持现状）

### 8.4 Revenue Manager（收益管理）

**功能**:
- 实时收益流（原生使用 + 衍生分润）
- 衍生收益追踪（级联关系图）
- 收益权拆分/交易
- 提现管理（ECHO→DCEP，T+1到账）

**衍生收益可视化**:
```
你的写诗助手
    ├── 被 营销文案助手 使用 → 你获得 15% = 0.003 ECHO/次
    ├── 被 古文翻译工具 引用 → 你获得 引用收益
    └── 被 诗歌教学助手 引用 → 你获得 引用收益

本月被动收入: 3.2 ECHO（占总额25%）
```

---

## 九、跨层协作流程

### 9.1 资产完整生命周期

```
创作者 ──→ 编写四权蓝图（YAML/可视化）
    │
    │      系统同时写入:
    │      ├── PostgreSQL: blueprints表（事务保证）
    │      ├── Kafka: AssetCreated事件（不可变记录）
    │      └── Redis: 初始状态（坤卦/潜藏期）
    │
    ▼
系统 ────→ 1. 语法校验（Schema验证）
    │      2. 初始状态计算 → 坤卦（潜藏期）
    │      3. 返回AssetID
    │
    ▼
用户 ────→ 发起使用请求
    │
    ▼
沙箱 ────→ 1. 查询有效配置（Redis → PostgreSQL）
    │      2. 校验权限（用权/余额/64卦约束）
    │      3. 执行Skill（Docker容器）
    │      4. 生成使用事件
    │
    ├─────→ Kafka: UsageOccurred事件（PoU载体）
    │         ├── 谁用的、何时、怎么用、付多少
    │         └── 沙箱签名 + 用户签名
    │
    └─────→ Redis: 累计指标 +1（为变势做准备）
    │
    ▼
定时任务 ──→ 每小时计算ShiGraph
    │
    ├─────→ 读Redis: 累计指标
    ├──→ 计算: T[2][2][2] = 咸卦（大成期）
    └──→ 对比原卦: 坤卦 → 咸卦（⚠️ 变势触发！）
    │
    ▼
约束引擎 ──→ 1. 查询物理法则: 咸卦要求衍≥3, 扩≥3
    │      2. 对比原配置: 衍=2（需调整）
    │      3. 仲裁决策: 衍强制调整为3
    │      4. Kafka: PolicyAdjusted + ShiStateChanged事件
    │      5. 通知创作者: 三级预警（已提前告知）
    │
    ▼
衍生者 ──→ 基于原资产创建新资产
    │
    ├─────→ 查询原蓝图: 衍=3（允许衍生），衍生费15%
    ├─────→ 创建新蓝图: parent=skill_001, 新配置
    └─────→ Kafka: DerivativeCreated事件
    │
    ▼
用户使用衍生资产
    │
    ├─────→ 支付0.02 ECHO
    └─────→ 自动分配: 85%衍生者 + 15%原创者 + 验证者 + 协议
    │
    ▼
Kafka: RevenueDistributed事件
    └──→ 原创者账户: +0.003 ECHO
```

### 9.2 数据一致性保证

**Outbox Pattern**:
```
1. 写入PostgreSQL（业务表 + 事件缓冲表，同一事务）
2. 后台任务读取事件缓冲表，发送到Kafka
3. 发送成功后，删除缓冲表记录
4. 定时对账任务修复不一致

优点: 利用PostgreSQL事务保证原子性
缺点: 最终一致，非实时
```

**Saga模式（执行原子性）**:
```
正向流程: 扣费 → 执行 → 记录事件

失败处理（如果记录失败）:
├── 不撤销扣费和执行（已发生）
└── 追加 CompensationEvent 到事件流
    └── 标记: "本次使用记录缺失，原因: 网络故障"

查询时: fold(事件流) 时识别补偿事件，调整统计值
```

---

## 十、实施路线图

### 10.1 三阶段路线图

```
Phase 1: 种子期（0-6个月）—— 主权边界验证
┌─────────────────────────────────────────────────────────────────┐
│ 目标: 验证核心假设，建立创世生态                                  │
├─────────────────────────────────────────────────────────────────┤
│ • 开发核心合约（EchoCore + 三注册表）                             │
│ • 部署3个沙箱节点（核心团队运营）                                 │
│ • 开发创世100个Skill（覆盖核心能力）                              │
│ • 验证PoU机制（使用→证明→验证→结算）                             │
│ • 开发Blueprint Studio MVP（L1-L2引导）                           │
│ • 开发Asset Dashboard（基础看板）                                 │
│ • DCEP托管方案落地（银行合作/第三方支付）                         │
│                                                                 │
│ 里程碑:                                                         │
│ ├── 100个Skill上线                                              │
│ ├── 日活跃使用 > 1000次                                         │
│ ├── PoU验证通过率 > 99%                                         │
│ └── 创作者首笔收益到账                                          │
└─────────────────────────────────────────────────────────────────┘

Phase 2: 成长期（6-12个月）—— 生态涌现
┌─────────────────────────────────────────────────────────────────┐
│ 目标: 开放网络，引入第三方，关系网络形成                          │
├─────────────────────────────────────────────────────────────────┤
│ • 开放沙箱注册（第三方创作者可部署）                              │
│ • 引入第三方验证者（质押机制上线）                                │
│ • ShiGraph引擎上线（每小时计算）                                │
│ • 势场测绘系统上线（每周测绘）                                    │
│ • Agent编排引擎上线（基础编排）                                   │
│ • 关系网络发现上线（Phase 1: 语义+协同过滤）                    │
│ • 收益权市场开放（成熟期Skill可交易收益权）                       │
│ • 社区治理启动（团队70%决策权）                                   │
│                                                                 │
│ 里程碑:                                                         │
│ ├── 1000+ Skill                                                 │
│ ├── 日活跃使用 > 10,000次                                       │
│ ├── 第三方验证者 > 50个                                         │
│ ├── 关系网络密度 > 0.1（平均每Skill连接数）                     │
│ └── 首例衍生作品级联分润                                        │
└─────────────────────────────────────────────────────────────────┘

Phase 3: 自治期（12个月+）—— 完全去中心化
┌─────────────────────────────────────────────────────────────────┐
│ 目标: 社区自治，协议自运行                                        │
├─────────────────────────────────────────────────────────────────┤
│ • 完全去中心化治理（团队30%决策权）                               │
│ • 纯关系网络发现（Phase 3: 无分类）                               │
│ • ZKP方案全面部署（Risc0/SP1）                                   │
│ • 跨链互操作（与其他链的资产互通）                                │
│ • 投资者市场成熟（收益权交易活跃）                                │
│ • 协议参数完全社区治理                                            │
│                                                                 │
│ 里程碑:                                                         │
│ ├── 10,000+ Skill                                               │
│ ├── 日活跃使用 > 100,000次                                      │
│ ├── 验证者网络 > 500个                                          │
│ ├── 关系网络密度 > 0.5                                          │
│ └── 社区治理提案 > 100个/年                                     │
└─────────────────────────────────────────────────────────────────┘
```

### 10.2 技术栈建议

| 组件 | MVP方案 | 完整方案 |
|------|---------|----------|
| **链上** | EVM兼容链（低Gas） | 自有L2或AppChain |
| **事件流** | Kafka | Kafka + Pulsar（多租户） |
| **数据库** | PostgreSQL | PostgreSQL + ClickHouse（分析） |
| **缓存** | Redis | Redis Cluster |
| **存储** | IPFS | IPFS + Arweave（永久存储） |
| **沙箱** | Docker + gVisor | Kata + WASM多级别 |
| **ZKP** | 普通签名（MVP跳过） | Risc0/SP1 |
| **API网关** | Kong | Kong + Envoy |
| **监控** | Prometheus + Grafana | + ELK + Jaeger |

### 10.3 关键指标

| 指标 | Phase 1目标 | Phase 2目标 | Phase 3目标 |
|------|-------------|-------------|-------------|
| Skill数量 | 100 | 1,000 | 10,000+ |
| 日活跃使用 | 1,000 | 10,000 | 100,000+ |
| 验证者数量 | 5（核心团队） | 50 | 500+ |
| 关系网络密度 | 0.01 | 0.1 | 0.5+ |
| 创作者月收入中位数 | 100 ECHO | 500 ECHO | 2,000 ECHO |
| PoU验证通过率 | 99% | 99.5% | 99.9% |
| 平均结算延迟 | <5s | <2s | <1s |

---

## 十一、附录

### 11.1 配套与核心概念的映射

| ECHO核心概念 | 对应配套设施 | 设施角色 |
|-------------|-------------|----------|
| **势枢 (三维编织)** | ShiGraph Engine + ShiShuRegistry | 计算+存储三维坐标 |
| **势场 (64卦聚类)** | ShiField Mapper + ShiFieldRegistry | 每周测绘+链上记录 |
| **势位 (六爻坐标)** | ShiPositionRegistry + Asset Dashboard | 存储+展示势位状态 |
| **四权 (用/衍/扩/益)** | Blueprint Studio + Policy Engine | 编辑+计算有效配置 |
| **使用证明 (PoU)** | PoU Engine + Validator Network | 生成+验证使用证明 |
| **收益分配** | Revenue Splitter + Revenue Manager | 自动分润+展示追踪 |
| **关系网络** | Relation Explorer + Agent Router | 发现+编排关联 |
| **创作者主权** | Creator Sandbox + Blueprint Studio | 私域执行+权利配置 |

### 11.2 配套与角色的映射

| 角色 | 主要使用设施 | 核心需求 |
|------|-------------|----------|
| **创作者** | Blueprint Studio + Asset Dashboard + Revenue Manager | 配置权利、观测状态、管理收益 |
| **用户** | Agent Router + Skill Market + User Wallet | 发现Skill、编排工作流、支付 |
| **衍生者** | Blueprint Studio + Relation Explorer | 查询上游权利、配置衍生规则 |
| **投资者** | Benefit Market + Asset Dashboard | 购买收益权、追踪投资回报 |
| **验证者** | Validator Node + Staking UI | 验证PoU、获得收益 |
| **开发者** | ShiGraph SDK + Sandbox SDK + Event API | 集成协议、开发Skill |
| **治理者** | Governance Portal + ShiField Mapper | 参与投票、监控生态 |

### 11.3 设计原则检查清单

- [ ] 该设施是否尊重创作者主权？（ECHO是否进入创作者私域？）
- [ ] 该设施是否保护隐私？（是否掌握资产数据明文？）
- [ ] 该设施是否透明？（规则是否公开可审计？）
- [ ] 该设施是否去中心化？（是否有单点控制？）
- [ ] 该设施是否激励相容？（各方是否有动力参与？）
- [ ] 该设施是否BA模式？（是否接受近似而非追求完美？）
- [ ] 该设施是否每一层都有价值？（能否独立使用？）

---

*万物有灵，皆可生长。*
*主权划定边界，协议保障权益，生态孕育价值。*
